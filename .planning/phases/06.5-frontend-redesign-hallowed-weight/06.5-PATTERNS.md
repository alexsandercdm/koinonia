# Phase 6.5: Frontend Redesign (Hallowed Weight) — Pattern Map

**Mapped:** 2026-04-25
**Files analyzed:** 26 (config + css + html + 12 primitivas + 3 layout + 1 context + 1 rota + 7 páginas + App.tsx)
**Analogs found:** 20 / 26

> **Naming clarification:** O prompt de invocação menciona `components/ui/` para primitivas. O UI-SPEC.md aprovado e o RESEARCH.md trancam o diretório como `components/ui-hw/` (decisão D1 / UX-03). O planner deve usar `components/ui-hw/` exclusivamente.

---

## Execution Waves (from RESEARCH.md — planning order)

1. **Wave 1 — Foundation:** `tailwind.config.js` + `index.css` + `index.html`
2. **Wave 2 — Primitivas:** 12 arquivos em `components/ui-hw/` + `index.ts`
3. **Wave 3 — AppShell + Rotas:** `AppShell.tsx` + `Sidebar.tsx` + `EventPill.tsx` + `event-context.tsx` + `App.tsx`
4. **Wave 4 — Páginas:** 7 páginas + `EventSelectorScreen.tsx`

---

## File Classification

| Arquivo novo/modificado | Role | Data Flow | Analog mais próximo | Qualidade |
|-------------------------|------|-----------|---------------------|-----------|
| `apps/web/tailwind.config.js` | config | transform | si mesmo (rewrite) | self |
| `apps/web/src/index.css` | config | transform | si mesmo (rewrite) | self |
| `apps/web/index.html` | config | — | si mesmo (edição) | self |
| `components/ui-hw/Btn.tsx` | component | request-response | `components/ui/button.tsx` | role-match |
| `components/ui-hw/Input.tsx` | component | request-response | `components/ui/input.tsx` | exact |
| `components/ui-hw/SelectInput.tsx` | component | request-response | `components/ui/input.tsx` | partial |
| `components/ui-hw/TextArea.tsx` | component | request-response | `components/ui/input.tsx` | partial |
| `components/ui-hw/FormField.tsx` | component | request-response | nenhum | none |
| `components/ui-hw/Card.tsx` | component | request-response | `components/ui/card.tsx` | exact |
| `components/ui-hw/EmptyState.tsx` | component | request-response | nenhum | none |
| `components/ui-hw/Drawer.tsx` | component | event-driven | `components/ui/sheet.tsx` | exact |
| `components/ui-hw/SectionTitle.tsx` | component | request-response | nenhum | none |
| `components/ui-hw/QRCodeSVG.tsx` | component | transform | nenhum | none |
| `components/ui-hw/FilterTabs.tsx` | component | event-driven | `pages/ParticipantsPage.tsx` FilterPill | partial |
| `components/ui-hw/Badge.tsx` | component | request-response | `components/ui/card.tsx` (forwardRef pattern) | partial |
| `components/ui-hw/Icon.tsx` | component | request-response | `AppLayout.tsx` span.material-symbols | partial |
| `components/ui-hw/index.ts` | config | — | nenhum barrel existente | none |
| `components/layout/AppShell.tsx` | component | request-response | `components/layout/AppLayout.tsx` | exact |
| `components/layout/Sidebar.tsx` | component | event-driven | `AppLayout.tsx` aside (linhas 26-83) | exact |
| `components/layout/EventPill.tsx` | component | event-driven | nenhum | none |
| `contexts/event-context.tsx` | provider | event-driven | `contexts/auth-context.tsx` | exact |
| `apps/web/src/App.tsx` | config | request-response | si mesmo (edição) | self |
| `pages/EventSelectorScreen.tsx` | component | request-response | `pages/login.tsx` (shape de layout) | partial |
| `pages/login.tsx` | component | request-response | si mesmo (rewrite) | self |
| `pages/register.tsx` | component | request-response | si mesmo (rewrite) | self |
| `pages/dashboard.tsx` + 4 páginas | component | CRUD | si mesmas (migração) | self |

---

## Pattern Assignments

### `apps/web/tailwind.config.js` (config, transform)

**Analog:** si mesmo — rewrite total.

**Estrutura atual a preservar** (linhas 1-10):
```js
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ["class"],   // <-- REMOVER "class" (HW é sistema light)
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
  ],
```

**Tokens Stitch a REMOVER** (linhas 54-59):
```js
// DELETAR estas 5 linhas:
"surface-dark": "#1b0f23",
"surface-elevated": "#291736",
"border-dark": "#2d1b3d",
"background-dark": "#0f0814",
"amber-accent": "#ffbf00",
```

**Tokens HW a ADICIONAR** em `theme.extend.colors` (fonte: RESEARCH.md `## Code Examples`):
```js
bg: '#F7F4EF', surface: '#FFFFFF', 'surface-raised': '#FDFCFA',
border: '#E8E3D9', 'border-strong': '#CFC8BB',
text: '#1A1612', 'text-sec': '#7A7060', 'text-ter': '#B0A898',
gold: '#C4923A', 'gold-light': '#F5ECD9', 'gold-muted': '#EDD8AD',
success: '#3D7A52', 'success-light': '#E8F4ED',
warning: '#B07030', 'warning-light': '#FBF0E2',
danger: '#9B3A2E', 'danger-light': '#FBECEC',
info: '#2E628B', 'info-light': '#E8F2FA',
```

**Spacing e borderRadius a ADICIONAR** em `theme.extend`:
```js
spacing: {
  'card-pad': '20px', 'main-pad': '28px', 'header-h': '56px',
  'sidebar-w': '232px', 'header-px': '24px', 'gap-cards': '14px',
},
borderRadius: {
  btn: '7px', card: '12px', pill: '99px', icon: '8px',
},
```

**Keyframes a ADICIONAR**:
```js
keyframes: {
  pulse: { '0%,100%': { opacity: '1' }, '50%': { opacity: '0.4' } },
  spin: { to: { transform: 'rotate(360deg)' } },
  // manter accordion-down e accordion-up existentes
},
animation: {
  pulse: 'pulse 1.5s ease infinite',
  spin: 'spin 0.8s linear infinite',
},
```

**Container** — mudar `"2xl": "1400px"` para `"2xl": "1100px"`.

---

### `apps/web/src/index.css` (config, transform)

**Analog:** si mesmo — rewrite total.

**Estrutura a manter** (linhas 1-3):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

**Bloco a REMOVER** (linhas 5-38) — todo o `:root, .dark { ... }` Midnight Koinonia.

**Bloco a ADICIONAR** — CSS vars HW light (remap para shadcn variable names, per D1):
```css
@layer base {
  :root {
    --background: 42 30% 95%;   /* #F7F4EF */
    --foreground: 23 11% 9%;    /* #1A1612 */
    --card: 0 0% 100%;          /* #FFFFFF */
    --card-foreground: 23 11% 9%;
    --primary: 23 11% 9%;       /* text (near-black) */
    --primary-foreground: 0 0% 100%;
    --accent: 36 55% 50%;       /* gold #C4923A */
    --accent-foreground: 0 0% 100%;
    --border: 40 24% 87%;       /* #E8E3D9 */
    --input: 40 24% 87%;
    --ring: 38 52% 75%;         /* gold-muted #EDD8AD */
    --radius: 0.4375rem;        /* 7px = rounded-btn */
  }
}
```

**Adicionar classe Icon** (Material Symbols):
```css
@layer utilities {
  .ms { font-family: 'Material Symbols Outlined'; font-variation-settings: 'FILL' 0; }
  .ms-fill { font-variation-settings: 'FILL' 1; }
}
```

---

### `apps/web/index.html` (config, —)

**Edição cirúrgica em 2 pontos:**

1. Linha 2 — remover `class="dark"`: `<html lang="pt-BR">` (sem class).

2. Após linha 8 (link Material Symbols) — adicionar DM Sans (fonte: RESEARCH.md `## Code Examples`):
```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet" />
```

---

### `components/ui-hw/Btn.tsx` (component, request-response)

**Analog:** `components/ui/button.tsx`

**Imports pattern** (button.tsx linhas 1-2):
```tsx
import React from 'react'
import { cn } from '../../lib/utils'
```
Para Btn adicionar: `import { cva, type VariantProps } from 'class-variance-authority'`

**Core pattern — forwardRef + cn** (button.tsx linhas 9-36):
```tsx
export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant = 'default', size = 'default', ...props }, ref) => {
    return (
      <button className={cn(...variants..., className)} ref={ref} {...props} />
    )
  }
)
Button.displayName = 'Button'
```

**CVA pattern** (fonte: RESEARCH.md `## Code Examples` Btn primitive):
```tsx
const btnVariants = cva(
  'inline-flex items-center gap-1.5 rounded-btn font-medium leading-none transition-all duration-120 ease focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold-muted focus-visible:ring-offset-2 disabled:bg-border disabled:text-text-ter disabled:cursor-not-allowed',
  {
    variants: {
      variant: {
        primary:   'bg-text text-white hover:bg-[#2E2820]',
        secondary: 'bg-transparent text-text border border-border hover:bg-surface-raised',
        gold:      'bg-gold text-white hover:bg-[#A87B2F]',
        ghost:     'bg-transparent text-text-sec hover:bg-[#F0EDE8]',
        danger:    'bg-danger-light text-danger border border-[#F0CACA] hover:bg-[#F5DADA]',
      },
      size: {
        sm: 'py-1.5 px-3 text-[13px]',
        md: 'py-2 px-4 text-[14px]',
        lg: 'py-[11px] px-[22px] text-[15px]',
      },
    },
    defaultVariants: { variant: 'primary', size: 'md' },
  }
)
```

Props adicionais HW: `icon?: string` (Material Symbol name, renderizado antes do children).

---

### `components/ui-hw/Input.tsx` (component, request-response)

**Analog:** `components/ui/input.tsx` — exact match.

**Core pattern** (input.tsx linhas 6-22):
```tsx
export const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, ...props }, ref) => (
    <input
      type={type}
      className={cn(
        // substituir classes Stitch por HW:
        'flex h-10 w-full rounded-btn border border-border bg-surface px-3 py-2 text-[14px] text-text placeholder:text-text-ter transition-all duration-150 ease',
        'focus-visible:outline-none focus-visible:ring-0 focus-visible:border-gold-muted focus-visible:shadow-[0_0_0_3px_theme(colors.gold-light)]',
        'disabled:cursor-not-allowed disabled:opacity-50',
        className
      )}
      ref={ref}
      {...props}
    />
  )
)
```

Props adicionais HW: `icon?: string` (ícone à esquerda), `error?: boolean`.

---

### `components/ui-hw/Card.tsx` (component, request-response)

**Analog:** `components/ui/card.tsx` — exact match.

**Core pattern** (card.tsx linhas 4-16):
```tsx
const Card = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(
  ({ className, ...props }, ref) => (
    <div
      ref={ref}
      className={cn(
        'rounded-card border border-border bg-surface p-[card-pad]',
        'transition-all duration-150 ease',
        className
      )}
      {...props}
    />
  )
)
```

---

### `components/ui-hw/Drawer.tsx` (component, event-driven)

**Analog:** `components/ui/sheet.tsx` — exact match (rewrap).

**Core pattern** (sheet.tsx linhas 1-8, 30-44):
```tsx
import * as DialogPrimitive from '@radix-ui/react-dialog'
// Sheet = DialogPrimitive.Root  — manter
// SheetTrigger = DialogPrimitive.Trigger  — manter
```

**Side pattern** (sheet.tsx linhas 33, 42-43):
```tsx
// Forçar side="right" por padrão no Drawer HW:
side === 'right' &&
  'right-0 top-0 h-full w-full max-w-sm data-[state=closed]:slide-out-to-right data-[state=open]:slide-in-from-right',
```

**Overlay** (sheet.tsx linhas 13-22) — substituir `bg-black/40` por `bg-text/20` (HW).

Adicionar `DrawerHeader`, `DrawerTitle`, `DrawerFooter` como exports nomeados.

---

### `components/ui-hw/FilterTabs.tsx` (component, event-driven)

**Analog:** `pages/ParticipantsPage.tsx` FilterPill (linhas 55-65):
```tsx
function FilterPill({ label, active, onClick }: FilterPillProps) {
  return (
    <button
      onClick={onClick}
      className={[
        'px-4 py-1.5 rounded-full text-sm font-medium transition-all',
        active ? '...' : '...',
      ].join(' ')}
    >
      {label}
    </button>
  )
}
```

**Tradução HW:** trocar `rounded-full` por `rounded-pill`, tokens ativos = `bg-gold-light text-gold`, inativos = `bg-transparent text-text-sec hover:bg-surface-raised`. Wrapper `<nav role="tablist">` com `flex gap-1`.

---

### `components/ui-hw/Icon.tsx` (component, request-response)

**Analog:** `AppLayout.tsx` linhas 54-58:
```tsx
<span
  className="material-symbols-outlined text-[20px]"
  style={isActive ? { fontVariationSettings: "'FILL' 1" } : undefined}
>
  {item.icon}
</span>
```

**Tradução HW:**
```tsx
interface IconProps { name: string; size?: number; fill?: boolean; className?: string }
function Icon({ name, size = 20, fill = false, className = '' }: IconProps) {
  return (
    <span
      className={cn('material-symbols-outlined leading-none select-none', className)}
      style={{ fontSize: size, ...(fill ? { fontVariationSettings: "'FILL' 1" } : {}) }}
    >
      {name}
    </span>
  )
}
```

---

### `components/layout/AppShell.tsx` + `Sidebar.tsx` (component, request-response)

**Analog:** `components/layout/AppLayout.tsx` — substituição completa.

**Estrutura raiz** (AppLayout.tsx linha 24):
```tsx
// Atual: bg-background-dark / Stitch
<div className="flex h-screen w-full overflow-hidden bg-background-dark">
// Alvo HW:
<div className="flex h-screen w-full overflow-hidden bg-bg">
```

**Sidebar** (AppLayout.tsx linhas 26-83) — padrão a preservar:
- `useNavigate` + `useLocation` para active state (linhas 1-2, 45-51)
- `fontVariationSettings: "'FILL' 1"` no ícone ativo (linha 55)
- Pattern de `NAV_ITEMS` array (linhas 4-10)

**Dimensões HW:** `w-[sidebar-w]` (232px) vs atual `w-64` (256px). Header `h-[header-h]` (56px) vs atual `h-16` (64px).

**EventPill slot** — inserir entre PageTitle e spacer no header (novo — sem analog).

**Props de AppShell:**
```tsx
interface AppShellProps {
  children: React.ReactNode
  title?: string
  actions?: React.ReactNode
}
```
(Mesma assinatura que AppLayout, para migração drop-in.)

---

### `contexts/event-context.tsx` (provider, event-driven)

**Analog:** `contexts/auth-context.tsx` — exact match de estrutura.

**Pattern completo** (auth-context.tsx linhas 1-38):
```tsx
// 1. createContext com tipo | undefined
const EventContext = createContext<EventContextType | undefined>(undefined)

// 2. Provider wraps state logic
export function EventProvider({ children }: { children: ReactNode }) {
  // estado local aqui (localStorage init)
  return <EventContext.Provider value={...}>{children}</EventContext.Provider>
}

// 3. Hook com throw-on-missing
export function useEventContext() {
  const context = useContext(EventContext)
  if (context === undefined) throw new Error('useEventContext must be used within an EventProvider')
  return context
}
```

**Estado específico** (fonte: RESEARCH.md Pattern 3):
```tsx
const STORAGE_KEY = 'koinonia.selectedEventoId'
const [selectedEventoId, setSelectedEventoId] = React.useState<string | null>(
  () => localStorage.getItem(STORAGE_KEY)
)
```

**Integração em App.tsx:** envolver `<AuthProvider>` com `<EventProvider>` após.

---

### `apps/web/src/App.tsx` (config, request-response)

**Analog:** si mesmo — edição de 3 linhas.

**Import a adicionar** (após linha 4):
```tsx
import { EventProvider } from './contexts/event-context'
import { EventSelectorScreen } from './pages/EventSelectorScreen'
```

**Rota a adicionar** (após linha 21, rota `/login`):
```tsx
<Route
  path="/eventos/selecionar"
  element={
    <ProtectedRoute>
      <EventSelectorScreen />
    </ProtectedRoute>
  }
/>
```

**Provider wrap** — `<EventProvider>` como filho de `<AuthProvider>` (linha 18).

**Redirect pós-login:** ProtectedRoute deve verificar `selectedEventoId === null` e redirecionar para `/eventos/selecionar`. Ver seção Shared Patterns abaixo.

---

### `pages/EventSelectorScreen.tsx` (component, request-response)

**Analog parcial:** `pages/login.tsx` — estrutura de página full-viewport sem AppShell.

**Pattern de layout autônomo** (login.tsx linhas 42-47):
```tsx
<div className="relative min-h-screen w-full flex flex-col items-center justify-center ...">
```

**Alvo HW:** `<div className="min-h-screen bg-bg flex flex-col">` — sem sidebar/header.

**Fonte canônica:** `doc/Koinonia-redesign/koinonia-page-eventos.jsx` (referência JSX para EventSelectorScreen).

**Dados via TanStack Query:**
```tsx
// Padrão de ParticipantsPage.tsx linhas 19-25:
function useEventos() {
  return useQuery({
    queryKey: ['eventos'],
    queryFn: () => apiFetch<EventoListItem[]>('/api/v1/eventos'),
    staleTime: 1000 * 60 * 5,
  })
}
```

---

### 7 Páginas de migração (dashboard, login, register, ParticipantsPage, InscricoesPage, AcomodacoesPage, FinanceiroPage)

**Analog:** si mesmas — migração in-place.

**Checklist por página (4 itens obrigatórios):**
1. Substituir todos imports `lucide-react` por `<Icon name="..." />` da `components/ui-hw`
2. Remover `font-black` → usar `font-semibold` (600) no máximo
3. Trocar tokens Stitch por tokens HW (ver tabela de mapeamento abaixo)
4. Adicionar `max-w-[1100px] mx-auto` no wrapper de conteúdo (exceto AcomodacoesPage map view)

**Mapeamento de tokens Stitch → HW (buscar+substituir):**

| De (Stitch) | Para (HW) |
|-------------|-----------|
| `bg-background-dark` | `bg-bg` |
| `bg-surface-dark` | `bg-surface` |
| `bg-surface-elevated` | `bg-surface-raised` |
| `border-border-dark` | `border-border` |
| `text-amber-accent` | `text-gold` |
| `bg-amber-accent` | `bg-gold` |
| `text-white` (body) | `text-text` |
| `text-slate-400` | `text-text-sec` |
| `text-slate-500` | `text-text-ter` |
| `bg-primary` | `bg-text` (primary button) |
| `hover:bg-primary/10` | `hover:bg-surface-raised` |
| `rounded-xl` | `rounded-card` |
| `rounded-full` (pills) | `rounded-pill` |

**Login e Register — rewrite two-column:**
Fonte canônica: `doc/Koinonia-redesign/koinonia-page-login.jsx`.
Painel esquerdo: `flex-1 bg-surface border-r border-border p-[40px_56px]`.
Painel direito: `w-[460px] flex-shrink-0 p-[40px_56px]`.
Breakpoint mobile: `flex-col` abaixo de `lg:`.

---

## Shared Patterns

### `cn()` utility
**Fonte:** `lib/utils.ts` linhas 1-6
```tsx
import { type ClassValue, clsx } from "clsx"
import { twMerge } from "tailwind-merge"
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
```
**Aplicar em:** todos os 12 primitivos HW.

### Material Symbols icon fill
**Fonte:** `AppLayout.tsx` linha 55
```tsx
style={isActive ? { fontVariationSettings: "'FILL' 1" } : undefined}
```
**Aplicar em:** `Icon.tsx` primitive (prop `fill`), nav ativo em `Sidebar.tsx`.

### ProtectedRoute pattern
**Fonte:** `components/protected-route.tsx` linhas 10-37
```tsx
export function ProtectedRoute({ children }: ProtectedRouteProps) {
  const { isAuthenticated, isLoading } = useAuthContext()
  if (isLoading) return <LoadingSpinner />
  if (!isAuthenticated) return <Navigate to="/login" replace />
  return <>{children}</>
}
```
**Extensão para Phase 6.5:** adicionar verificação de `selectedEventoId` após `isAuthenticated`:
```tsx
const { selectedEventoId } = useEventContext()
if (!selectedEventoId) return <Navigate to="/eventos/selecionar" replace />
```

### TanStack Query data fetching
**Fonte:** `pages/ParticipantsPage.tsx` linhas 19-25
```tsx
useQuery({ queryKey: ['resource'], queryFn: () => apiFetch<T[]>('/api/v1/...'), staleTime: 1000 * 60 * 2 })
```
**Aplicar em:** `EventPill.tsx` (lista de eventos), `EventSelectorScreen.tsx`.

### Formatação de moeda PT-BR
**Fonte:** `pages/dashboard.tsx` linhas 36-39
```tsx
function formatCurrency(value: number): string {
  return value.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' })
}
```
**Aplicar em:** todas as páginas com valores financeiros (FinanceiroPage, Dashboard).

### React Hook Form + Zod validation
**Fonte:** `pages/login.tsx` linhas 1-15, 23-29
```tsx
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
const schema = z.object({ email: z.string().email(), password: z.string().min(6) })
const { register, handleSubmit, formState: { errors } } = useForm({ resolver: zodResolver(schema) })
```
**Aplicar em:** `login.tsx` (rewrite), `register.tsx` (rewrite).

---

## No Analog Found

| Arquivo | Role | Data Flow | Motivo |
|---------|------|-----------|--------|
| `components/ui-hw/FormField.tsx` | component | request-response | Nenhum FormField existente — criar do zero seguindo spec L150 |
| `components/ui-hw/EmptyState.tsx` | component | request-response | Nenhum padrão de empty state — criar seguindo spec L182 e UI-SPEC copywriting |
| `components/ui-hw/SectionTitle.tsx` | component | request-response | Nenhum eyebrow/section title component — criar seguindo spec L230 |
| `components/ui-hw/QRCodeSVG.tsx` | component | transform | Sem analog. Custom seed-based SVG — spec L239. Sem lib externa (per Don't Hand-Roll). |
| `components/layout/EventPill.tsx` | component | event-driven | Sem analog. Fonte canônica: `doc/Koinonia-redesign/koinonia-layout.jsx` EventPill section. |
| `components/ui-hw/index.ts` | config | — | Barrel export — criar com todos os 12 named exports |

---

## Metadata

**Analog search scope:** `apps/web/src/` — components/ui/, components/layout/, contexts/, pages/, lib/
**Files scanned:** 14 arquivos lidos diretamente
**Pattern extraction date:** 2026-04-25
**Fontes canônicas para primitivas sem analog:** `doc/Koinonia-redesign/koinonia-primitives.jsx`, `doc/Koinonia-redesign/koinonia-layout.jsx`, `doc/Koinonia-redesign/koinonia-page-login.jsx`, `doc/Koinonia-redesign/koinonia-page-eventos.jsx`, `doc/Koinonia-redesign/koinonia-pages-main.jsx`
