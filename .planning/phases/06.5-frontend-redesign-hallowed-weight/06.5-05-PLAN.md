---
phase: 06.5
plan: "05"
type: execute
wave: 2
depends_on:
  - "06.5-02"
  - "06.5-03"
  - "06.5-04"
files_modified:
  - apps/web/src/components/ui-hw/index.ts
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "index.ts exporta exatamente os 13 items: Btn, Input, SelectInput, TextArea, FormField, Card, Badge, EmptyState, SectionTitle, Drawer+exports, FilterTabs, Icon, QRCodeSVG"
    - "Importações de páginas e AppShell podem usar import { Btn, Icon, Card } from '../components/ui-hw'"
  artifacts:
    - path: "apps/web/src/components/ui-hw/index.ts"
      provides: "Barrel export de todas as 12 primitivas HW + Icon"
      exports: ["Btn", "Input", "SelectInput", "TextArea", "FormField", "Card", "Badge", "EmptyState", "SectionTitle", "Drawer", "DrawerTrigger", "DrawerClose", "DrawerContent", "DrawerHeader", "DrawerTitle", "DrawerBody", "DrawerFooter", "FilterTabs", "Icon", "QRCodeSVG"]
  key_links:
    - from: "apps/web/src/components/ui-hw/index.ts"
      to: "all 12 primitive files"
      via: "re-export named exports"
      pattern: "export.*from"
---

<objective>
Criar o barrel export `index.ts` que consolida todas as 12 primitivas Hallowed Weight. Depende dos planos 02, 03 e 04 (todos os primitivos devem existir antes deste plano executar).

Purpose: Sem o barrel, cada página importaria de arquivos individuais. O barrel permite `import { Btn, Icon, Card } from '../components/ui-hw'` em todas as páginas e AppShell.
Output: Um único arquivo index.ts com todos os re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar index.ts — barrel export de todas as primitivas ui-hw</name>
  <read_first>
    - apps/web/src/components/ui-hw/ (listar arquivos criados pelos planos 02, 03, 04 para confirmar todos existem)
  </read_first>
  <files>apps/web/src/components/ui-hw/index.ts</files>
  <action>
Verificar que todos os 12 arquivos primitivos existem em `apps/web/src/components/ui-hw/`:
- Btn.tsx, Input.tsx, SelectInput.tsx, TextArea.tsx, FormField.tsx (plano 02)
- Card.tsx, Badge.tsx, EmptyState.tsx, SectionTitle.tsx (plano 03)
- Drawer.tsx, FilterTabs.tsx, Icon.tsx, QRCodeSVG.tsx (plano 04)

Criar `apps/web/src/components/ui-hw/index.ts`:

```ts
// Form primitives
export { Btn } from './Btn'
export type { BtnProps } from './Btn'

export { Input } from './Input'
export type { InputProps } from './Input'

export { SelectInput } from './SelectInput'
export type { SelectInputProps } from './SelectInput'

export { TextArea } from './TextArea'
export type { TextAreaProps } from './TextArea'

export { FormField } from './FormField'
export type { FormFieldProps } from './FormField'

// Display primitives
export { Card } from './Card'
export type { CardProps } from './Card'

export { Badge } from './Badge'
export type { BadgeProps } from './Badge'

export { EmptyState } from './EmptyState'
export type { EmptyStateProps } from './EmptyState'

export { SectionTitle } from './SectionTitle'
export type { SectionTitleProps } from './SectionTitle'

// Layout / utility primitives
export {
  Drawer,
  DrawerTrigger,
  DrawerClose,
  DrawerContent,
  DrawerHeader,
  DrawerTitle,
  DrawerBody,
  DrawerFooter,
} from './Drawer'
export type { DrawerContentProps } from './Drawer'

export { FilterTabs } from './FilterTabs'
export type { FilterTabsProps, FilterTab } from './FilterTabs'

export { Icon } from './Icon'
export type { IconProps } from './Icon'

export { QRCodeSVG } from './QRCodeSVG'
export type { QRCodeSVGProps } from './QRCodeSVG'
```

**Regra:** Este arquivo APENAS re-exporta — nenhuma lógica, nenhum import de terceiros.
  </action>
  <verify>
    <automated>grep -c "^export" apps/web/src/components/ui-hw/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts existe em `apps/web/src/components/ui-hw/`
    - index.ts contém export de `Btn`, `Input`, `SelectInput`, `TextArea`, `FormField`
    - index.ts contém export de `Card`, `Badge`, `EmptyState`, `SectionTitle`
    - index.ts contém export de `Drawer`, `DrawerTrigger`, `DrawerContent`, `DrawerHeader`, `DrawerTitle`, `DrawerBody`, `DrawerFooter`
    - index.ts contém export de `FilterTabs`, `Icon`, `QRCodeSVG`
    - index.ts NÃO contém lógica de componente (apenas re-exports)
    - `pnpm --filter @koinonia/web type-check` passa sem erros relacionados a ui-hw
  </acceptance_criteria>
  <done>Barrel export consolidando todas as 12 primitivas HW + Icon. Imports curtos habilitados em toda a aplicação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Module resolution | Barrel export — sem trust boundary relevante (apenas re-exports) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-11 | Tampering | index.ts barrel | accept | Barrel é build-time only — sem runtime surface. Risco zero. |
</threat_model>

<verification>
```bash
# Verificar barrel criado
cat apps/web/src/components/ui-hw/index.ts | grep "^export" | wc -l
# deve ser >= 20 (12 componentes + type exports)

# Type check final do diretório
pnpm --filter @koinonia/web type-check
```
</verification>

<success_criteria>
- index.ts existe com ≥20 linhas de export
- Todas as 12 primitivas nomeadas estão exportadas
- Todos os types de props também exportados
- pnpm type-check não tem erros de resolução em ui-hw
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-05-SUMMARY.md`
</output>
