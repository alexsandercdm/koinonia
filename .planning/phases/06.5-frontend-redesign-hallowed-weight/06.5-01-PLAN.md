---
phase: 06.5
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/tailwind.config.js
  - apps/web/src/index.css
  - apps/web/index.html
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "Tokens cream/gold/black disponíveis via classes Tailwind (bg-bg, bg-surface, text-text, text-gold, etc.)"
    - "Tokens Stitch (surface-dark, background-dark, amber-accent, border-dark, surface-elevated) ausentes do config"
    - "DM Sans carregada com pesos 300/400/500/600/700 + 400 italic via Google Fonts"
    - "class=\"dark\" removido do html — página não carrega em dark mode"
    - "CSS vars HW light mapeadas para nomes shadcn (--background, --foreground, --primary, --accent, --border, --ring)"
  artifacts:
    - path: "apps/web/tailwind.config.js"
      provides: "Token definitions HW — colors, spacing, borderRadius, keyframes"
      contains: "gold:"
    - path: "apps/web/src/index.css"
      provides: "CSS custom properties HW + Material Symbols utilities"
      contains: "--background:"
    - path: "apps/web/index.html"
      provides: "DM Sans font load + class dark removal"
      contains: "DM+Sans"
  key_links:
    - from: "apps/web/tailwind.config.js"
      to: "apps/web/src/index.css"
      via: "HSL values must match between Tailwind tokens and CSS custom props"
      pattern: "F7F4EF"
    - from: "apps/web/index.html"
      to: "apps/web/tailwind.config.js"
      via: "fontFamily.sans must resolve to DM Sans after load"
      pattern: "DM.Sans"
---

<objective>
Foundation layer for the Hallowed Weight redesign: replace Stitch tokens with HW tokens in Tailwind config, rewrite CSS custom properties, load DM Sans, and remove the dark-mode HTML class.

Purpose: All subsequent waves (primitives, AppShell, pages) consume these tokens. No downstream component can be built correctly until this layer is in place.
Output: Updated tailwind.config.js, index.css, index.html — immediately observable by stopping dark background.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-UI-SPEC.md
@.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-PATTERNS.md
@doc/Koinonia-redesign/HANDOFF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tailwind.config.js — substituir tokens Stitch por tokens HW</name>
  <read_first>
    - apps/web/tailwind.config.js (arquivo completo — entender estrutura atual antes de reescrever)
  </read_first>
  <files>apps/web/tailwind.config.js</files>
  <action>
Reescrever o arquivo `apps/web/tailwind.config.js` preservando a estrutura `export default { ... }` e o array `content`, mas aplicando as seguintes mudanças:

1. **darkMode:** remover `["class"]` — substituir por `false` (HW é sistema light-only).

2. **theme.extend.colors — DELETAR** os 5 tokens Stitch (linhas 54-59):
   ```js
   "surface-dark": "#1b0f23",
   "surface-elevated": "#291736",
   "border-dark": "#2d1b3d",
   "background-dark": "#0f0814",
   "amber-accent": "#ffbf00",
   ```

3. **theme.extend.colors — ADICIONAR** tokens HW completos:
   ```js
   bg: '#F7F4EF',
   surface: '#FFFFFF',
   'surface-raised': '#FDFCFA',
   border: '#E8E3D9',
   'border-strong': '#CFC8BB',
   text: '#1A1612',
   'text-sec': '#7A7060',
   'text-ter': '#B0A898',
   gold: '#C4923A',
   'gold-light': '#F5ECD9',
   'gold-muted': '#EDD8AD',
   success: '#3D7A52',
   'success-light': '#E8F4ED',
   warning: '#B07030',
   'warning-light': '#FBF0E2',
   danger: '#9B3A2E',
   'danger-light': '#FBECEC',
   info: '#2E628B',
   'info-light': '#E8F2FA',
   ```

4. **theme.extend.spacing — ADICIONAR** semantic tokens:
   ```js
   'card-pad': '20px',
   'main-pad': '28px',
   'header-h': '56px',
   'sidebar-w': '232px',
   'header-px': '24px',
   'gap-cards': '14px',
   ```

5. **theme.extend.borderRadius — ADICIONAR** (manter os existentes, adicionar HW):
   ```js
   btn: '7px',
   card: '12px',
   pill: '99px',
   icon: '8px',
   ```

6. **theme.extend.fontFamily — ADICIONAR** (se não existir):
   ```js
   sans: ['DM Sans', 'system-ui', '-apple-system', 'sans-serif'],
   ```

7. **theme.extend.keyframes — ADICIONAR** (manter accordion-down/up existentes):
   ```js
   pulse: { '0%,100%': { opacity: '1' }, '50%': { opacity: '0.4' } },
   spin: { to: { transform: 'rotate(360deg)' } },
   ```

8. **theme.extend.animation — ADICIONAR**:
   ```js
   pulse: 'pulse 1.5s ease infinite',
   spin: 'spin 0.8s linear infinite',
   ```

9. **Container:** alterar `screens["2xl"]` de `"1400px"` para `"1100px"`.
  </action>
  <verify>
    <automated>grep -c "gold:" apps/web/tailwind.config.js && grep -c "surface-dark" apps/web/tailwind.config.js || true</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contém `gold: '#C4923A'`
    - tailwind.config.js contém `'surface-raised': '#FDFCFA'`
    - tailwind.config.js contém `'card-pad': '20px'`
    - tailwind.config.js contém `'sidebar-w': '232px'`
    - tailwind.config.js contém `btn: '7px'`
    - tailwind.config.js NÃO contém `surface-dark`
    - tailwind.config.js NÃO contém `background-dark`
    - tailwind.config.js NÃO contém `amber-accent`
    - tailwind.config.js NÃO contém `["class"]` no darkMode
    - grep `"1100px"` retorna match (container max-w)
  </acceptance_criteria>
  <done>Todos os tokens HW registrados; todos os tokens Stitch removidos; container em 1100px.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite index.css — CSS vars HW light + utilitários Material Symbols</name>
  <read_first>
    - apps/web/src/index.css (arquivo completo — entender bloco :root atual antes de reescrever)
  </read_first>
  <files>apps/web/src/index.css</files>
  <action>
Reescrever `apps/web/src/index.css`. Manter as 3 primeiras linhas (`@tailwind base/components/utilities`). Remover completamente o bloco `:root, .dark { ... }` Midnight Koinonia. Substituir por:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 42 30% 95%;        /* #F7F4EF cream */
    --foreground: 23 11% 9%;         /* #1A1612 near-black */
    --card: 0 0% 100%;               /* #FFFFFF white */
    --card-foreground: 23 11% 9%;    /* #1A1612 */
    --popover: 0 0% 100%;
    --popover-foreground: 23 11% 9%;
    --primary: 23 11% 9%;            /* #1A1612 — primary button bg */
    --primary-foreground: 0 0% 100%; /* white text on primary */
    --secondary: 40 24% 87%;         /* #E8E3D9 border */
    --secondary-foreground: 23 11% 9%;
    --muted: 40 19% 95%;             /* #FDFCFA surface-raised */
    --muted-foreground: 32 10% 46%;  /* #7A7060 text-sec */
    --accent: 36 55% 50%;            /* #C4923A gold */
    --accent-foreground: 0 0% 100%;
    --destructive: 7 53% 39%;        /* #9B3A2E danger */
    --destructive-foreground: 0 0% 100%;
    --border: 40 24% 87%;            /* #E8E3D9 */
    --input: 40 24% 87%;
    --ring: 38 52% 75%;              /* #EDD8AD gold-muted */
    --radius: 0.4375rem;             /* 7px = rounded-btn */
  }

  * {
    border-color: hsl(var(--border));
  }

  body {
    background-color: hsl(var(--background));
    color: hsl(var(--foreground));
    font-family: 'DM Sans', system-ui, -apple-system, sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
}

@layer utilities {
  .ms {
    font-family: 'Material Symbols Outlined';
    font-variation-settings: 'FILL' 0, 'wght' 300, 'GRAD' 0, 'opsz' 20;
    font-style: normal;
    line-height: 1;
    letter-spacing: normal;
    text-transform: none;
    display: inline-block;
    white-space: nowrap;
    word-wrap: normal;
    direction: ltr;
    -webkit-font-feature-settings: 'liga';
    font-feature-settings: 'liga';
    -webkit-font-smoothing: antialiased;
    user-select: none;
  }

  .ms-fill {
    font-variation-settings: 'FILL' 1, 'wght' 300, 'GRAD' 0, 'opsz' 20;
  }
}
```

Remover quaisquer referências a `class="dark"`, `.dark`, variáveis HSL do Midnight Koinonia (purple/violet).
  </action>
  <verify>
    <automated>grep -c "\-\-background:" apps/web/src/index.css && grep -c "surface-dark\|Midnight\|midnight" apps/web/src/index.css || true</automated>
  </verify>
  <acceptance_criteria>
    - index.css contém `--background: 42 30% 95%`
    - index.css contém `--accent: 36 55% 50%`
    - index.css contém `--ring: 38 52% 75%`
    - index.css contém `.ms {` (classe utilitária Material Symbols)
    - index.css contém `.ms-fill {`
    - index.css NÃO contém `.dark` (bloco dark mode removido)
    - index.css NÃO contém `4d0085` (violet Stitch removido)
    - `@tailwind base;` está na linha 1
  </acceptance_criteria>
  <done>CSS vars HW light registradas; dark mode CSS removido; utilitários .ms e .ms-fill disponíveis.</done>
</task>

<task type="auto">
  <name>Task 3: Editar index.html — carregar DM Sans + remover class="dark"</name>
  <read_first>
    - apps/web/index.html (arquivo completo — localizar linha com class="dark" e posição do link Material Symbols)
  </read_first>
  <files>apps/web/index.html</files>
  <action>
Editar `apps/web/index.html` com duas mudanças cirúrgicas:

**Mudança 1 — remover `class="dark"` do `<html>`:**
Localizar a tag `<html lang="pt-BR" class="dark">` (ou similar com class="dark") e remover o atributo `class="dark"` inteiro, resultando em `<html lang="pt-BR">`.

**Mudança 2 — adicionar DM Sans após o link do Material Symbols existente:**
Após a linha que carrega Material Symbols (`https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined`), inserir:

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet" />
```

Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>grep -c "DM+Sans" apps/web/index.html && grep -c 'class="dark"' apps/web/index.html || true</automated>
  </verify>
  <acceptance_criteria>
    - index.html contém `DM+Sans` (Google Fonts link presente)
    - index.html contém `rel="preconnect" href="https://fonts.googleapis.com"`
    - index.html NÃO contém `class="dark"` (atributo removido do html tag)
    - index.html ainda contém Material Symbols link (não foi removido)
    - `<html` tag agora tem apenas `lang="pt-BR"` sem class adicional
  </acceptance_criteria>
  <done>DM Sans carregada; dark mode class removida; Material Symbols preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time config → runtime CSS | tailwind.config.js tokens compilados para CSS — sem dados de usuário neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-01 | Tampering | tailwind.config.js darkMode removal | accept | Remoção de `["class"]` no darkMode é intencional e não expõe superfície de ataque — HW é light-only |
| T-06.5-02 | Information Disclosure | Google Fonts CDN (index.html) | accept | Font load via CDN é padrão; não expõe dados de usuário; fallback system-ui garante funcionalidade offline |
</threat_model>

<verification>
Após execução deste plano:

```bash
# Token HW presentes
grep "gold:" apps/web/tailwind.config.js
grep "surface-raised" apps/web/tailwind.config.js

# Token Stitch ausentes
grep "surface-dark\|background-dark\|amber-accent\|surface-elevated\|border-dark" apps/web/tailwind.config.js
# deve retornar vazio (exit code 1)

# DM Sans carregada
grep "DM.Sans" apps/web/index.html

# Dark mode removido
grep 'class="dark"' apps/web/index.html
# deve retornar vazio

# Build type-check
pnpm --filter @koinonia/web type-check
```
</verification>

<success_criteria>
- tailwind.config.js contém os 20 tokens de cor HW (bg, surface, surface-raised, border, border-strong, text, text-sec, text-ter, gold, gold-light, gold-muted, success/warning/danger/info + light variants)
- tailwind.config.js NÃO contém tokens Stitch (surface-dark, background-dark, amber-accent, border-dark, surface-elevated)
- index.css contém CSS vars shadcn mapeadas para paleta HW (--background: 42 30% 95%)
- index.html carrega DM Sans via Google Fonts
- index.html NÃO tem class="dark"
- pnpm --filter @koinonia/web type-check passa sem erros críticos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-01-SUMMARY.md`
</output>
