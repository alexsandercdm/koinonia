---
phase: 06.5
plan: "10"
type: execute
wave: 4
depends_on:
  - "06.5-07"
  - "06.5-09"
files_modified:
  - apps/web/src/pages/dashboard.tsx
  - apps/web/src/pages/ParticipantsPage.tsx
  - apps/web/src/pages/InscricoesPage.tsx
  - apps/web/src/pages/AcomodacoesPage.tsx
  - apps/web/src/pages/FinanceiroPage.tsx
  - apps/web/package.json
autonomous: true
requirements:
  - UX-07
  - UX-02
must_haves:
  truths:
    - "5 páginas (Dashboard, Participantes, Inscrições, Acomodações, Financeiro) usam tokens HW — sem tokens Stitch"
    - "lucide-react removido do package.json após grep audit confirmar zero imports restantes"
    - "Todas as páginas têm max-w-[1100px] mx-auto no wrapper de conteúdo (exceto AcomodacoesPage map view)"
    - "Todas as páginas usam primitivas ui-hw (Btn, Card, Badge, EmptyState, FilterTabs, Icon)"
  artifacts:
    - path: "apps/web/src/pages/dashboard.tsx"
      provides: "Dashboard migrado para HW — EventBanner + KPIs + tabelas"
      contains: "max-w-[1100px]"
    - path: "apps/web/src/pages/ParticipantsPage.tsx"
      provides: "ParticipantsPage migrado — tabela com FilterTabs HW"
      contains: "max-w-[1100px]"
    - path: "apps/web/src/pages/InscricoesPage.tsx"
      provides: "InscricoesPage migrado — KPIs + tabela com badges de status"
      contains: "max-w-[1100px]"
    - path: "apps/web/src/pages/AcomodacoesPage.tsx"
      provides: "AcomodacoesPage migrado — tokens HW (map view sem max-w)"
      contains: "bg-surface"
    - path: "apps/web/src/pages/FinanceiroPage.tsx"
      provides: "FinanceiroPage migrado — KPIs + progress bar gold"
      contains: "max-w-[1100px]"
  key_links:
    - from: "apps/web/src/pages/dashboard.tsx"
      to: "apps/web/src/components/ui-hw/index.ts"
      via: "Importa Card, Badge, Icon, EmptyState das primitivas HW"
      pattern: "from '../components/ui-hw'"
    - from: "apps/web/package.json"
      to: "apps/web/src/"
      via: "lucide-react removido após grep audit = 0"
      pattern: "lucide-react"
---

<objective>
Migrar as 5 páginas internas (Dashboard, Participantes, Inscrições, Acomodações, Financeiro) dos tokens Stitch para tokens HW e primitivas ui-hw. Ao final, executar grep audit e remover lucide-react do package.json.

Purpose: Este é o último plano — após sua conclusão, todos os 7 critérios de sucesso da fase estarão atendidos. A remoção de lucide-react é o marco final de UX-02.
Output: 5 páginas migradas + lucide-react removido do package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-UI-SPEC.md
@.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-PATTERNS.md
@doc/Koinonia-redesign/HANDOFF.md
@.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-07-SUMMARY.md

<interfaces>
Token migration map (from PATTERNS.md):
```
bg-background-dark → bg-bg
bg-surface-dark → bg-surface
bg-surface-elevated → bg-surface-raised
border-border-dark → border-border
text-amber-accent → text-gold
bg-amber-accent → bg-gold
text-slate-400 → text-text-sec
text-slate-500 → text-text-ter
bg-primary → bg-text
rounded-xl → rounded-card
rounded-full (pills) → rounded-pill
```

From apps/web/src/components/ui-hw/index.ts:
```tsx
import { Btn, Card, Badge, EmptyState, FilterTabs, Icon, SectionTitle } from '../components/ui-hw'
```

Lucide → Material Symbols icon map:
```
Users → people
UserPlus → person_add
Search → search
Filter → filter_list
MoreVertical → more_vert
ChevronRight → chevron_right
TrendingUp → trending_up
DollarSign → payments
AlertCircle → warning
CheckCircle → check_circle
X → close
Plus → add
Edit → edit
Trash → delete
Bed → bed
Home → home
Calendar → calendar_month
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar dashboard.tsx e ParticipantsPage.tsx</name>
  <read_first>
    - apps/web/src/pages/dashboard.tsx (arquivo completo — mapear todos os tokens Stitch e imports lucide)
    - apps/web/src/pages/ParticipantsPage.tsx (arquivo completo — mapear todos os tokens Stitch, FilterPill a substituir por FilterTabs)
    - doc/Koinonia-redesign/koinonia-pages-main.jsx (fonte canônica — Dashboard e Participantes layout)
    - .planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-UI-SPEC.md (seção Page-level patterns — Dashboard e Participantes)
  </read_first>
  <files>
    apps/web/src/pages/dashboard.tsx
    apps/web/src/pages/ParticipantsPage.tsx
  </files>
  <action>
**Para cada arquivo, executar 4 operações obrigatórias:**

**Operação 1 — Remover imports lucide-react:**
Substituir cada ícone lucide pelo padrão Material Symbols inline ou `<Icon>`:
```tsx
// Remover: import { Users, Search, ... } from 'lucide-react'
// Substituir uso: <Users size={20} /> → <Icon name="people" size={20} />
```
Usar o mapa de ícones na seção `<interfaces>` acima para correspondências.

**Operação 2 — Substituir tokens Stitch por tokens HW:**
Usar search-replace com a tabela de PATTERNS.md:
```
bg-background-dark → bg-bg
bg-surface-dark → bg-surface
border-border-dark → border-border
text-amber-accent → text-gold
bg-amber-accent → bg-gold
text-slate-400 → text-text-sec
text-slate-500 → text-text-ter
bg-primary → bg-text (em botões)
rounded-xl → rounded-card
```

**Operação 3 — Remover font-black:**
Substituir `font-black` → `font-semibold` (weight 600).

**Operação 4 — Adicionar max-w-[1100px] mx-auto:**
Verificar wrapper raiz de conteúdo. Se não tiver `max-w-[1100px] mx-auto`, adicionar.

**Específico para dashboard.tsx:**
- Importar primitivas: `import { Card, Badge, Icon, EmptyState } from '../components/ui-hw'`
- EventBanner: `Card` com `border-l-[3px] border-l-gold` (stripe gold)
- KPI cards: `Card` com `p-[card-pad]`
- "Ver todas" link: `text-[13px] text-gold hover:text-[#A87B2F]`
- Eyebrow `EVENTO EM ANDAMENTO`: `text-[11px] font-semibold text-gold uppercase tracking-[0.1em]`

**Específico para ParticipantsPage.tsx:**
- Substituir `FilterPill` customizado por `<FilterTabs>` da ui-hw
- Converter: `<FilterPill label="Todos" active={filter === 'all'} onClick={() => setFilter('all')} />` → array `tabs` + `<FilterTabs tabs={...} active={filter} onChange={setFilter} />`
- Tabela: header `bg-surface-raised`, row hover `hover:bg-surface-raised`
- Busca: `<Input icon="search" placeholder="Buscar participante..." value={search} onChange={e => setSearch(e.target.value)} />`
- CTA "Novo participante": `<Btn icon="person_add">Novo participante</Btn>`
  </action>
  <verify>
    <automated>grep -c "lucide" apps/web/src/pages/dashboard.tsx && grep -c "lucide" apps/web/src/pages/ParticipantsPage.tsx || true</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.tsx NÃO contém `lucide`
    - dashboard.tsx NÃO contém `surface-dark`, `background-dark`, `amber-accent`
    - dashboard.tsx contém `max-w-[1100px] mx-auto`
    - dashboard.tsx contém `import { Card, Badge, Icon` de `'../components/ui-hw'`
    - ParticipantsPage.tsx NÃO contém `lucide`
    - ParticipantsPage.tsx NÃO contém tokens Stitch
    - ParticipantsPage.tsx contém `FilterTabs` import e uso (não mais FilterPill manual)
    - ParticipantsPage.tsx contém `max-w-[1100px] mx-auto`
    - Nenhum arquivo contém `font-black`
    - `pnpm --filter @koinonia/web type-check` passa
  </acceptance_criteria>
  <done>dashboard.tsx e ParticipantsPage.tsx migrados para tokens HW, sem lucide, com max-w-1100px.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar InscricoesPage.tsx, AcomodacoesPage.tsx e FinanceiroPage.tsx</name>
  <read_first>
    - apps/web/src/pages/InscricoesPage.tsx (arquivo completo)
    - apps/web/src/pages/AcomodacoesPage.tsx (arquivo completo)
    - apps/web/src/pages/FinanceiroPage.tsx (arquivo completo)
    - doc/Koinonia-redesign/koinonia-pages-main.jsx (Inscrições e Financeiro layout)
    - doc/Koinonia-redesign/koinonia-page-acomodacoes.jsx (Acomodações layout — preservar estrutura de quartos)
  </read_first>
  <files>
    apps/web/src/pages/InscricoesPage.tsx
    apps/web/src/pages/AcomodacoesPage.tsx
    apps/web/src/pages/FinanceiroPage.tsx
  </files>
  <action>
Aplicar as 4 operações idênticas ao Task 1 em cada arquivo:
1. Remover lucide-react (mapa de ícones disponível em `<interfaces>`)
2. Substituir tokens Stitch → HW (tabela de PATTERNS.md)
3. Remover font-black → font-semibold
4. Adicionar max-w-[1100px] mx-auto (exceto AcomodacoesPage map view)

**Específico para InscricoesPage.tsx:**
- KPI cards horizontais: `<Card>` com `p-3 flex items-center gap-3`
- Status badges: `<Badge variant="success">Pago</Badge>`, `<Badge variant="warning">Parcial</Badge>`, `<Badge variant="danger">Pendente</Badge>` — mapear de STATUS_MAP
- FilterTabs com `tabs={[{id:'todos', label:'Todos'}, ...]}` se há filtros por status
- CTA "Nova inscrição": `<Btn icon="person_add">Nova inscrição</Btn>`

**Específico para AcomodacoesPage.tsx:**
- Map view: NÃO adicionar max-w-[1100px] (view de quartos precisa de largura plena — per UI-SPEC)
- Substituir tokens de cor (dark → light) mas preservar estrutura de quartos/camas
- Camas: de cor dark → cores HW (livre → `bg-surface-raised border border-border`, ocupado → `bg-gold-light border border-gold-muted`)
- Header de contexto: `text-[15px] font-semibold text-text`

**Específico para FinanceiroPage.tsx:**
- KPI grid: 4 colunas com `Card` + `gap-cards` (token 14px)
- Progress bar financeiro: `bg-gold` no fill (meta financeira = contexto goal = gold permitido)
- "Progresso da meta financeira": porcentagem `text-[28px] font-bold text-text tabular-nums`
- Eyebrow acima da porcentagem: `SectionTitle` component
- CTA "Registrar": `<Btn icon="add">Registrar</Btn>`

**Após todos os 5 arquivos migrados — audit de gold:**
Verificar que nenhuma página tem mais de 5 ocorrências de gold tokens:
```bash
for f in dashboard.tsx ParticipantsPage.tsx InscricoesPage.tsx AcomodacoesPage.tsx FinanceiroPage.tsx; do
  count=$(grep -c "text-gold\|bg-gold\|border-gold\|gold-light\|gold-muted" "apps/web/src/pages/$f" || echo 0)
  echo "$f: $count gold occurrences"
done
```
Se algum arquivo tiver >5, revisar e justificar ou remover usos desnecessários.
  </action>
  <verify>
    <automated>grep -c "lucide" apps/web/src/pages/InscricoesPage.tsx && grep -c "lucide" apps/web/src/pages/FinanceiroPage.tsx || true</automated>
  </verify>
  <acceptance_criteria>
    - InscricoesPage.tsx NÃO contém `lucide`
    - InscricoesPage.tsx contém `Badge variant="success"`, `variant="warning"`, `variant="danger"`
    - InscricoesPage.tsx contém `max-w-[1100px] mx-auto`
    - AcomodacoesPage.tsx NÃO contém `lucide`
    - AcomodacoesPage.tsx NÃO contém tokens Stitch
    - AcomodacoesPage.tsx NÃO tem `max-w-[1100px]` (map view — exceção documentada)
    - FinanceiroPage.tsx NÃO contém `lucide`
    - FinanceiroPage.tsx contém `bg-gold` no progress bar de meta financeira
    - FinanceiroPage.tsx contém `max-w-[1100px] mx-auto`
    - Nenhum dos 3 contém `font-black`
    - `pnpm --filter @koinonia/web type-check` passa
  </acceptance_criteria>
  <done>InscricoesPage, AcomodacoesPage e FinanceiroPage migrados para tokens HW, sem lucide, sem font-black.</done>
</task>

<task type="auto">
  <name>Task 3: Audit final + remover lucide-react do package.json</name>
  <read_first>
    - apps/web/package.json (linha com lucide-react — confirmar versão antes de remover)
  </read_first>
  <files>apps/web/package.json</files>
  <action>
**Passo 1 — Auditoria obrigatória antes de remover:**
Executar grep para confirmar zero imports de lucide em todo o src/:
```bash
grep -r "lucide-react" apps/web/src/ --include="*.tsx" --include="*.ts"
```

Se a grep retornar QUALQUER resultado:
- Abrir o arquivo encontrado
- Substituir o import e uso pelo padrão Material Symbols (`<Icon name="...">` ou `<span className="material-symbols-outlined">`)
- Re-executar a grep até retornar vazio

**Passo 2 — Remover lucide-react do package.json:**
Apenas quando grep retornar zero resultados, editar `apps/web/package.json`:
- Remover a linha `"lucide-react": "^0.363.0"` (ou versão que estiver) das `dependencies`

**Passo 3 — Reinstalar dependências:**
```bash
pnpm --filter @koinonia/web install
```
ou (se pnpm workspace root):
```bash
pnpm install
```

**Passo 4 — Type-check final completo:**
```bash
pnpm --filter @koinonia/web type-check
```

**Passo 5 — Audit de gold tokens por página:**
```bash
for f in dashboard.tsx ParticipantsPage.tsx InscricoesPage.tsx AcomodacoesPage.tsx FinanceiroPage.tsx; do
  count=$(grep -c "text-gold\|bg-gold\|border-gold\|gold-light\|gold-muted" "apps/web/src/pages/$f" 2>/dev/null || echo 0)
  echo "$f: $count gold tokens"
done
```
Regra: ≤5 por arquivo. Se algum tiver >5, revisar e justificar.

**Passo 6 — Audit de tokens Stitch (deve ser zero em todo src/):**
```bash
grep -r "surface-dark\|background-dark\|amber-accent\|surface-elevated\|border-dark" apps/web/src/ --include="*.tsx" --include="*.ts" --include="*.css"
```
Deve retornar vazio.
  </action>
  <verify>
    <automated>grep -r "lucide-react" apps/web/src/ --include="*.tsx" --include="*.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "lucide-react" apps/web/src/` retorna ZERO linhas
    - apps/web/package.json NÃO contém `lucide-react` na seção `dependencies`
    - `pnpm --filter @koinonia/web type-check` exits com código 0
    - `grep -r "surface-dark\|background-dark\|amber-accent" apps/web/src/` retorna ZERO linhas
    - Audit de gold: nenhuma página tem >5 gold tokens (ou overuse está justificado)
    - `grep -c "font-black" apps/web/src/` retorna ZERO (sem `wc -l`, usando grep -c diretamente)
  </acceptance_criteria>
  <done>lucide-react removido do package.json e de todo apps/web/src/. Audit Stitch tokens = 0. pnpm type-check limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data → page tables/KPIs | Nomes/valores da API renderizados nas páginas — auto-escapados por React |
| User interaction → page actions | Buttons com callbacks de navegação — sem input livre de usuário neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-25 | Information Disclosure | Dashboard KPIs — dados financeiros | accept | Dados são protegidos por ProtectedRoute + Better Auth session. Sem exposição adicional na migração visual |
| T-06.5-26 | Tampering | AcomodacoesPage — bed assignment UI | accept | Lógica de atribuição de camas (validação de gênero, transação) está no backend — migração visual não altera behavior |
| T-06.5-27 | Denial of Service | pnpm install após remoção de lucide | accept | lucide-react sendo removido não quebra tree-shaking de outras libs — risco de incompatibilidade é mínimo e reversível |
</threat_model>

<verification>
```bash
# AUDIT FINAL — todos devem retornar 0 ou vazio

# 1. Lucide ausente
grep -r "lucide-react" apps/web/src/ --include="*.tsx" --include="*.ts"

# 2. Tokens Stitch ausentes
grep -r "surface-dark\|background-dark\|amber-accent\|surface-elevated\|border-dark" apps/web/src/

# 3. font-black ausente
grep -r "font-black" apps/web/src/

# 4. Package.json sem lucide
grep "lucide" apps/web/package.json

# 5. max-w-[1100px] presente
grep -l "max-w-\[1100px\]" apps/web/src/pages/*.tsx
# deve listar 4+ arquivos (exceto AcomodacoesPage)

# 6. Type check limpo
pnpm --filter @koinonia/web type-check
```
</verification>

<success_criteria>
- 5 páginas migradas: Dashboard, Participantes, Inscrições, Acomodações, Financeiro
- Zero lucide-react imports em todo apps/web/src/
- Zero tokens Stitch em todo apps/web/src/
- Zero font-black em todo apps/web/src/
- lucide-react removido de apps/web/package.json
- pnpm --filter @koinonia/web type-check passes (exit 0)
- max-w-[1100px] presente em todas as páginas exceto AcomodacoesPage map view
- Gold tokens ≤5 por página
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06.5-frontend-redesign-hallowed-weight/06.5-10-SUMMARY.md`
</output>
