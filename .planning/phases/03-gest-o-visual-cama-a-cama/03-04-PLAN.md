---
phase: 03-gest-o-visual-cama-a-cama
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - apps/web/src/pages/AcomodacoesPage.tsx
  - apps/web/src/components/ui/sheet.tsx
  - apps/web/src/components/acomodacoes/AssignCamaSheet.tsx
  - apps/web/src/components/acomodacoes/ExportMapaPdfButton.tsx
  - apps/web/src/lib/pdf/exportMapaAcomodacao.ts
  - apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx
  - apps/web/src/tests/acomodacoes-e2e.spec.ts
autonomous: true
requirements: [ACO-04, ACO-05, ACO-06, ACO-07]
user_setup: []
must_haves:
  truths:
    - "Admin e lider conseguem atribuir e liberar camas a partir do mapa sem refresh manual da pagina."
    - "Mensagens de erro da API para genero, cama ocupada e cama bloqueada aparecem na UI operacional."
    - "Servo continua em modo somente leitura e nao recebe controles de mutacao."
    - "O operador consegue exportar o mapa atual em PDF legivel offline, agrupado por quarto e sem depender de internet."
  artifacts:
    - "apps/web/src/components/ui/sheet.tsx"
    - "apps/web/src/components/acomodacoes/AssignCamaSheet.tsx"
    - "apps/web/src/components/acomodacoes/ExportMapaPdfButton.tsx"
    - "apps/web/src/lib/pdf/exportMapaAcomodacao.ts"
    - "apps/web/src/tests/acomodacoes-e2e.spec.ts"
  key_links:
    - "AssignCamaSheet consome `GET /api/v1/eventos/:eventoId/inscricoes-sem-cama?q=` e chama os endpoints de atribuir/liberar definidos no plan 03-02."
    - "AcomodacoesPage invalida as queries do mapa e da lista de candidatos apos atribuir ou liberar cama."
    - "ExportMapaPdfButton captura uma subtree imprimivel do mapa atual e gera um PDF com cabecalho do evento/local."
---

<objective>
Finalizar a experiencia operacional do modulo com atribuicao/liberacao de camas no mapa e exportacao do layout em PDF.

Purpose: transformar a pagina de acomodacoes em uma ferramenta de campo completa para o retiro, incluindo acao imediata e material offline.
Output: sheet de atribuicao, fluxo de liberacao, exportador de PDF do mapa atual e cobertura browser para a experiencia principal.
</objective>

<execution_context>
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-CONTEXT.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md
@doc/koinonia-doc-tec.md
@apps/web/package.json
@apps/web/src/pages/AcomodacoesPage.tsx
@apps/web/src/hooks/use-acomodacoes.ts
@apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx
@apps/web/src/tests/auth-e2e.spec.ts
@apps/web/playwright.config.ts
@.planning/phases/03-gest-o-visual-cama-a-cama/03-02-PLAN.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-03-PLAN.md

<interfaces>
From apps/web/package.json:
- html2canvas ^1.4.1
- jspdf ^2.5.1
- @radix-ui/react-dialog ^1.0.5

From 03-02 API plan:
- GET /api/v1/eventos/:eventoId/inscricoes-sem-cama?q=
- POST /api/v1/acomodacoes/camas/:camaId/atribuir
- DELETE /api/v1/acomodacoes/camas/:camaId/atribuir
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assignment and release controls to the accommodation map</name>
  <files>apps/web/src/components/ui/sheet.tsx, apps/web/src/components/acomodacoes/AssignCamaSheet.tsx, apps/web/src/pages/AcomodacoesPage.tsx, apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx</files>
  <read_first>apps/web/src/pages/AcomodacoesPage.tsx, apps/web/src/hooks/use-acomodacoes.ts, apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx, apps/web/package.json, .planning/phases/03-gest-o-visual-cama-a-cama/03-02-PLAN.md</read_first>
  <action>Create a reusable `sheet` primitive on top of `@radix-ui/react-dialog`, then implement `AssignCamaSheet.tsx` as the operator flow for bed assignment. Opening the sheet from a bed card must fetch `GET /api/v1/eventos/:eventoId/inscricoes-sem-cama?q=` and render a searchable list of eligible enrollments. For `admin` and `lider`, each `Disponivel` bed gets an "Atribuir" action; each `Ocupado` bed gets a "Liberar" action that calls the release endpoint with confirmation. `servo` must not see either action. After assignment or release, invalidate the map query and the candidate list query so the page reflects the new occupancy immediately without manual refresh.</action>
  <verify>rg -n "Atribuir|Liberar|inscricoes-sem-cama|invalidateQueries|sheet" apps/web/src/components/acomodacoes/AssignCamaSheet.tsx apps/web/src/pages/AcomodacoesPage.tsx apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx apps/web/src/components/ui/sheet.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/components/ui/sheet.tsx` exists and is used by `AssignCamaSheet.tsx`.
    - The UI only renders assignment/release controls for `admin` and `lider`.
    - Successful assign/release actions invalidate the relevant TanStack Query caches.
    - The map updates after mutation without full-page reload or manual refresh.
  </acceptance_criteria>
  <done>The web map is no longer read-only and supports the core `ACO-05` and `ACO-06` operations.</done>
</task>

<task type="auto">
  <name>Task 2: Surface backend guardrail errors and generate a printable PDF of the current map</name>
  <files>apps/web/src/components/acomodacoes/AssignCamaSheet.tsx, apps/web/src/components/acomodacoes/ExportMapaPdfButton.tsx, apps/web/src/lib/pdf/exportMapaAcomodacao.ts, apps/web/src/pages/AcomodacoesPage.tsx</files>
  <read_first>apps/web/package.json, apps/web/src/components/ui/button.tsx, apps/web/src/pages/AcomodacoesPage.tsx, doc/koinonia-doc-tec.md, .planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md</read_first>
  <action>In the assignment sheet, show inline operator-facing errors returned by the API for gender mismatch, occupied bed, blocked bed, or missing event/local context; do not collapse them into a generic failure toast. Implement `ExportMapaPdfButton.tsx` and `apps/web/src/lib/pdf/exportMapaAcomodacao.ts` to export only the printable map subtree. The PDF must use filename `${evento.nome}-mapa-acomodacao.pdf`, include event name, local name, export timestamp, room grouping, and a text legend for `Disponivel`, `Ocupado`, and `Bloqueado`. Use `html2canvas` with scale 2 and `jsPDF` in A4 portrait, and exclude interactive controls such as assign/release buttons from the captured output.</action>
  <verify>rg -n "html2canvas|jsPDF|mapa-acomodacao.pdf|Disponivel|Bloqueado|error" apps/web/src/lib/pdf/exportMapaAcomodacao.ts apps/web/src/components/acomodacoes/ExportMapaPdfButton.tsx apps/web/src/components/acomodacoes/AssignCamaSheet.tsx</verify>
  <acceptance_criteria>
    - The sheet displays specific API error messages for gender, occupancy, and blocked-bed conflicts.
    - `apps/web/src/lib/pdf/exportMapaAcomodacao.ts` uses both `html2canvas` and `jsPDF`.
    - The generated PDF file name ends with `-mapa-acomodacao.pdf`.
    - The exported content includes text legend and room grouping, not only color-based status cues.
  </acceptance_criteria>
  <done>The operator can both recover from backend validation errors and export an offline-readable room map for `ACO-07`.</done>
</task>

<task type="auto">
  <name>Task 3: Cover the accommodation page behavior with Playwright</name>
  <files>apps/web/src/tests/acomodacoes-e2e.spec.ts</files>
  <read_first>apps/web/src/tests/auth-e2e.spec.ts, apps/web/playwright.config.ts, apps/web/src/pages/AcomodacoesPage.tsx, apps/web/src/components/acomodacoes/AssignCamaSheet.tsx</read_first>
  <action>Create `apps/web/src/tests/acomodacoes-e2e.spec.ts` focused on browser-visible behavior. Cover at minimum: navigation from dashboard to `/acomodacoes`, visibility of room cards and status legend, absence of assign/release buttons for a mocked `servo`, presence of assignment controls for `lider` or `admin`, and presence of the PDF export affordance on the accommodation page. Keep the assertions UI-level; do not duplicate API business-rule tests already owned by plan `03-02`.</action>
  <verify>pnpm --filter @koinonia/web exec playwright test src/tests/acomodacoes-e2e.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `apps/web/src/tests/acomodacoes-e2e.spec.ts` exists and covers route entry plus main map controls.
    - The suite asserts `servo` has read-only behavior.
    - The suite asserts a role with write access sees assignment controls and the PDF export action.
    - The Playwright command in `<verify>` passes in Chromium.
  </acceptance_criteria>
  <done>The final accommodation experience is browser-regression-protected at the user-visible layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @koinonia/web type-check`
- [ ] `pnpm --filter @koinonia/web build`
- [ ] `pnpm --filter @koinonia/web exec playwright test src/tests/acomodacoes-e2e.spec.ts --project=chromium`
- [ ] Manual spot-check that export output omits interactive controls and remains legible in grayscale
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `ACO-05` and `ACO-06` are operable from the map itself with role-aware controls
- `ACO-07` is satisfied by client-side PDF export of the live room map
</success_criteria>

<output>
After completion, create `.planning/phases/03-gest-o-visual-cama-a-cama/03-04-SUMMARY.md`
</output>
