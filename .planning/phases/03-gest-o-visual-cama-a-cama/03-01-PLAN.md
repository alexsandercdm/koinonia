---
phase: 03-gest-o-visual-cama-a-cama
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/db/schema.ts
  - packages/shared/src/index.ts
  - apps/api/src/modules/acomodacoes/repositories/AcomodacaoRepository.ts
  - apps/api/src/modules/acomodacoes/controllers/AcomodacaoStructureController.ts
  - apps/api/src/modules/acomodacoes/routes/acomodacoes.ts
  - apps/api/src/app.ts
  - apps/api/src/tests/acomodacoes-estrutura.e2e.test.ts
autonomous: true
requirements: [ACO-01, ACO-02, ACO-03]
user_setup: []
must_haves:
  truths:
    - "Admin e lider conseguem manter a estrutura hierarquica local -> quarto -> cama via API autenticada."
    - "A estrutura cadastrada preserva genero permitido por quarto, tipo/identificacao por cama e capacidade em todos os niveis."
    - "Os contratos compartilhados do modulo de acomodacoes podem ser consumidos pela API e pela web sem tipos duplicados."
  artifacts:
    - "apps/api/src/modules/acomodacoes/routes/acomodacoes.ts"
    - "apps/api/src/modules/acomodacoes/controllers/AcomodacaoStructureController.ts"
    - "apps/api/src/modules/acomodacoes/repositories/AcomodacaoRepository.ts"
    - "apps/api/src/tests/acomodacoes-estrutura.e2e.test.ts"
    - "packages/shared/src/index.ts"
  key_links:
    - "apps/api/src/app.ts registra acomodacaoRoutes sob /api/v1 para expor CRUD estrutural reutilizavel pela web."
    - "packages/shared/src/index.ts exporta DTOs e enums de acomodacao usados nas rotas Fastify e nos hooks React."
---

<objective>
Criar a base estrutural do modulo de acomodacoes no backend, com contratos compartilhados e CRUD autenticado de locais, quartos e camas.

Purpose: destravar o restante da fase com uma fonte unica de verdade para a hierarquia fisica e para os DTOs do modulo.
Output: schema/DTOs de acomodacao atualizados, modulo Fastify de estrutura cadastral e suite E2E cobrindo o fluxo local -> quarto -> cama.
</objective>

<execution_context>
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-CONTEXT.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md
@doc/koinonia-doc-tec.md
@apps/api/src/db/schema.ts
@packages/shared/src/index.ts
@apps/api/src/modules/inscricoes/routes/inscricoes.ts
@apps/api/src/app.ts
@apps/api/src/tests/helpers/setupTestDB.ts

<interfaces>
From packages/shared/src/index.ts:
- GeneroQuartoEnum = ['M', 'F', 'MISTO']
- TipoCamaEnum = ['solteiro', 'beliche_superior', 'beliche_inferior', 'casal']
- UserRoleEnum = ['admin', 'lider', 'servo']

From apps/api/src/db/schema.ts:
- locais(id, nome, endereco, capacidade_total)
- quartos(id, local_id, nome, genero_permitido, capacidade)
- camas(id, quarto_id, identificacao, tipo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate accommodation persistence and shared DTO contracts</name>
  <files>apps/api/src/db/schema.ts, packages/shared/src/index.ts</files>
  <read_first>apps/api/src/db/schema.ts, packages/shared/src/index.ts, doc/koinonia-doc-tec.md, .planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md</read_first>
  <action>Update the persistence and shared-validation layer for the accommodation module without renaming existing core tables. In `apps/api/src/db/schema.ts`, add `camas.bloqueada` as `boolean('bloqueada').notNull().default(false)` so the map can represent the required `bloqueado` status. In `packages/shared/src/index.ts`, add `StatusCamaEnum = z.enum(['disponivel', 'ocupado', 'bloqueado'])`, plus explicit `CreateLocalDTO`, `UpdateLocalDTO`, `CreateQuartoDTO`, `UpdateQuartoDTO`, `CreateCamaDTO`, and `UpdateCamaDTO`. Also add a nested `MapaAcomodacaoSchema` response contract with `evento`, `local`, `quartos[]`, and `camas[]` so downstream API/UI plans implement against one shared shape.</action>
  <verify>rg -n "bloqueada|StatusCamaEnum|CreateLocalDTO|MapaAcomodacaoSchema" apps/api/src/db/schema.ts packages/shared/src/index.ts</verify>
  <acceptance_criteria>
    - `apps/api/src/db/schema.ts` contains `bloqueada` on `camas` with default `false`.
    - `packages/shared/src/index.ts` exports `StatusCamaEnum`.
    - `packages/shared/src/index.ts` exports create/update DTOs for local, quarto, and cama.
    - `packages/shared/src/index.ts` exports `MapaAcomodacaoSchema` with nested local/quarto/cama data.
  </acceptance_criteria>
  <done>Accommodation tables and shared DTO contracts cover structural CRUD plus future map status representation.</done>
</task>

<task type="auto">
  <name>Task 2: Expose authenticated CRUD routes for local, room, and bed maintenance</name>
  <files>apps/api/src/modules/acomodacoes/repositories/AcomodacaoRepository.ts, apps/api/src/modules/acomodacoes/controllers/AcomodacaoStructureController.ts, apps/api/src/modules/acomodacoes/routes/acomodacoes.ts, apps/api/src/app.ts</files>
  <read_first>apps/api/src/modules/inscricoes/routes/inscricoes.ts, apps/api/src/modules/inscricoes/controllers/InscricaoController.ts, apps/api/src/app.ts, apps/api/src/middleware/auth.ts, packages/shared/src/index.ts</read_first>
  <action>Create a new backend module at `apps/api/src/modules/acomodacoes/` that follows the same thin-route/controller/repository pattern used by inscricoes. Implement `AcomodacaoRepository` methods for creating, updating, and listing nested structure. Register `acomodacaoRoutes` from `apps/api/src/app.ts` under `/api/v1`. The route file must expose at least: `GET /api/v1/acomodacoes/locais`, `POST /api/v1/acomodacoes/locais`, `PATCH /api/v1/acomodacoes/locais/:localId`, `POST /api/v1/acomodacoes/locais/:localId/quartos`, `PATCH /api/v1/acomodacoes/quartos/:quartoId`, `POST /api/v1/acomodacoes/quartos/:quartoId/camas`, and `PATCH /api/v1/acomodacoes/camas/:camaId`. Allow `admin` and `lider` to write; keep reads available to authenticated users so `servo` can later consume the map without structure-edit access.</action>
  <verify>rg -n "/acomodacoes/locais|/acomodacoes/quartos|/acomodacoes/camas|acomodacaoRoutes" apps/api/src/modules/acomodacoes/routes/acomodacoes.ts apps/api/src/app.ts</verify>
  <acceptance_criteria>
    - `apps/api/src/modules/acomodacoes/routes/acomodacoes.ts` defines the seven structural endpoints listed in the action.
    - `apps/api/src/app.ts` registers the accommodation routes under `/api/v1`.
    - Write endpoints are protected so `servo` cannot create or edit local/quarto/cama records.
    - `GET /api/v1/acomodacoes/locais` returns nested room/bed structure instead of flat unrelated records.
  </acceptance_criteria>
  <done>The API exposes a reusable structural CRUD surface for accommodation management with the expected role boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the structural API with end-to-end coverage</name>
  <files>apps/api/src/tests/acomodacoes-estrutura.e2e.test.ts</files>
  <read_first>apps/api/src/tests/inscricoes-e2e.test.ts, apps/api/src/tests/helpers/setupTestDB.ts, apps/api/src/app.ts, apps/api/src/modules/acomodacoes/routes/acomodacoes.ts</read_first>
  <action>Create `apps/api/src/tests/acomodacoes-estrutura.e2e.test.ts` using `buildApp()` plus `clearDatabase()`. Cover: (1) create local with nome/endereco/capacidade_total, (2) create quarto nested under that local with `genero_permitido` and `capacidade`, (3) create cama nested under the room with `identificacao`, `tipo`, and default `bloqueada=false`, (4) list nested structure and assert the hierarchy is preserved in the JSON response, and (5) assert a `servo` token receives a 403-equivalent failure on structural writes.</action>
  <verify>pnpm --filter @koinonia/api exec vitest run src/tests/acomodacoes-estrutura.e2e.test.ts</verify>
  <acceptance_criteria>
    - `apps/api/src/tests/acomodacoes-estrutura.e2e.test.ts` exists and covers create/list flows for local, quarto, and cama.
    - The suite asserts `bloqueada` defaults to `false` on newly created beds.
    - The suite asserts unauthorized write attempts by `servo` fail.
    - The test command in `<verify>` passes without editing unrelated phase-1 or phase-2 tests.
  </acceptance_criteria>
  <done>The accommodation structure module is executable and regression-protected before map operations are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @koinonia/api type-check`
- [ ] `pnpm --filter @koinonia/api exec vitest run src/tests/acomodacoes-estrutura.e2e.test.ts`
- [ ] Manual spot-check that every structural route uses shared DTOs from `packages/shared/src/index.ts`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No auth or route registration regressions introduced in `apps/api/src/app.ts`
- Structural CRUD for `ACO-01`, `ACO-02`, and `ACO-03` is available through authenticated API endpoints with nested read shape
</success_criteria>

<output>
After completion, create `.planning/phases/03-gest-o-visual-cama-a-cama/03-01-SUMMARY.md`
</output>
