---
phase: 03-gest-o-visual-cama-a-cama
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/api/src/modules/acomodacoes/repositories/AcomodacaoRepository.ts
  - apps/api/src/modules/acomodacoes/controllers/AcomodacaoOperationsController.ts
  - apps/api/src/modules/acomodacoes/routes/acomodacoes.ts
  - apps/api/src/modules/acomodacoes/usecases/ListMapaAcomodacaoUseCase.ts
  - apps/api/src/modules/acomodacoes/usecases/ListInscricoesDisponiveisUseCase.ts
  - apps/api/src/modules/acomodacoes/usecases/AssignCamaUseCase.ts
  - apps/api/src/modules/acomodacoes/usecases/ReleaseCamaUseCase.ts
  - apps/api/src/tests/acomodacoes-operacoes.e2e.test.ts
autonomous: true
requirements: [ACO-04, ACO-05, ACO-06]
user_setup: []
must_haves:
  truths:
    - "O mapa operacional por evento retorna cada cama com status disponivel, ocupado ou bloqueado e com ocupante quando houver."
    - "A atribuicao de cama so aceita inscritos ativos do mesmo evento, sem cama previa e compatveis com o genero permitido do quarto."
    - "Liberar cama remove apenas `inscricoes.cama_id`, mantendo inscricao e historico financeiro intactos."
    - "Concorrencia de dupla atribuicao nao permite que duas inscricoes terminem com a mesma cama no mesmo evento."
  artifacts:
    - "apps/api/src/modules/acomodacoes/usecases/ListMapaAcomodacaoUseCase.ts"
    - "apps/api/src/modules/acomodacoes/usecases/AssignCamaUseCase.ts"
    - "apps/api/src/modules/acomodacoes/usecases/ReleaseCamaUseCase.ts"
    - "apps/api/src/tests/acomodacoes-operacoes.e2e.test.ts"
  key_links:
    - "AcomodacaoOperationsController usa `AcomodacaoRepository` e os use cases de mapa/atribuicao/liberacao para expor rotas consumiveis pela pagina web."
    - "O endpoint `GET /api/v1/eventos/:eventoId/mapa-acomodacao` devolve o shape de `MapaAcomodacaoSchema` definido no plan 03-01."
    - "Os endpoints de atribuicao e liberacao alteram somente `inscricoes.cama_id`, sem mutar status financeiro."
---

<objective>
Implementar a operacao principal do modulo de acomodacoes no backend: mapa por evento, busca de inscritos sem cama, atribuicao transacional e liberacao de cama.

Purpose: fechar as regras de negocio criticas antes da UI operacional, incluindo genero, disponibilidade, escopo do evento e concorrencia.
Output: endpoints operacionais do modulo, use cases transacionais e suite E2E cobrindo mapa, conflito de genero, ocupacao e liberacao.
</objective>

<execution_context>
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-CONTEXT.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md
@doc/koinonia-doc-tec.md
@apps/api/src/db/schema.ts
@apps/api/src/modules/inscricoes/repositories/EventoRepository.ts
@apps/api/src/modules/inscricoes/repositories/InscricaoRepository.ts
@apps/api/src/tests/inscricoes-e2e.test.ts
@.planning/phases/03-gest-o-visual-cama-a-cama/03-01-PLAN.md

<interfaces>
From packages/shared/src/index.ts after 03-01:
- MapaAcomodacaoSchema
- StatusCamaEnum
- GeneroQuartoEnum

From apps/api/src/db/schema.ts:
- inscricoes(evento_id, pessoa_id, cama_id, status)
- eventos(local_id)
- pessoas(genero)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return the live accommodation map and eligible enrollment search by event</name>
  <files>apps/api/src/modules/acomodacoes/repositories/AcomodacaoRepository.ts, apps/api/src/modules/acomodacoes/usecases/ListMapaAcomodacaoUseCase.ts, apps/api/src/modules/acomodacoes/usecases/ListInscricoesDisponiveisUseCase.ts, apps/api/src/modules/acomodacoes/controllers/AcomodacaoOperationsController.ts, apps/api/src/modules/acomodacoes/routes/acomodacoes.ts</files>
  <read_first>apps/api/src/db/schema.ts, packages/shared/src/index.ts, doc/koinonia-doc-tec.md, apps/api/src/modules/inscricoes/repositories/EventoRepository.ts, .planning/phases/03-gest-o-visual-cama-a-cama/03-01-PLAN.md</read_first>
  <action>Extend the accommodation repository to join `eventos`, `locais`, `quartos`, `camas`, `inscricoes`, and `pessoas` for event-scoped operations. Implement `GET /api/v1/eventos/:eventoId/mapa-acomodacao` returning the exact nested `MapaAcomodacaoSchema` shape. Each bed must expose `status` computed as: `bloqueado` when `camas.bloqueada=true`, `ocupado` when a non-cancelled enrollment in that event points to the bed, otherwise `disponivel`. Also implement `GET /api/v1/eventos/:eventoId/inscricoes-sem-cama?q=` that returns only active enrollments of the same event with `cama_id IS NULL`, searchable by participant name for the assignment sheet.</action>
  <verify>rg -n "mapa-acomodacao|inscricoes-sem-cama|bloqueado|ocupado|disponivel" apps/api/src/modules/acomodacoes/routes/acomodacoes.ts apps/api/src/modules/acomodacoes/usecases/ListMapaAcomodacaoUseCase.ts apps/api/src/modules/acomodacoes/usecases/ListInscricoesDisponiveisUseCase.ts</verify>
  <acceptance_criteria>
    - `GET /api/v1/eventos/:eventoId/mapa-acomodacao` is defined in `apps/api/src/modules/acomodacoes/routes/acomodacoes.ts`.
    - The map use case returns `evento`, `local`, `quartos`, and `camas` with computed status and occupant name when assigned.
    - The eligible-enrollments endpoint excludes records with `cama_id` already set.
    - Events without `local_id` fail with an explicit operator-facing error instead of returning a broken map.
  </acceptance_criteria>
  <done>The backend can power a live map page and an assignment picker from event-scoped data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transactional bed assignment and release with strict business guards</name>
  <files>apps/api/src/modules/acomodacoes/repositories/AcomodacaoRepository.ts, apps/api/src/modules/acomodacoes/usecases/AssignCamaUseCase.ts, apps/api/src/modules/acomodacoes/usecases/ReleaseCamaUseCase.ts, apps/api/src/modules/acomodacoes/controllers/AcomodacaoOperationsController.ts, apps/api/src/modules/acomodacoes/routes/acomodacoes.ts</files>
  <read_first>apps/api/src/db/schema.ts, doc/koinonia-doc-tec.md, .planning/phases/03-gest-o-visual-cama-a-cama/03-CONTEXT.md, apps/api/src/modules/inscricoes/repositories/InscricaoRepository.ts</read_first>
  <action>Implement `POST /api/v1/acomodacoes/camas/:camaId/atribuir` with body `{ inscricao_id }` and `DELETE /api/v1/acomodacoes/camas/:camaId/atribuir` for release. `AssignCamaUseCase` must run inside `db.transaction(...)`, lock the target bed row and the target enrollment row before update, then enforce all guards in this order: event has `local_id`; enrollment exists and belongs to the same event being operated; enrollment status is not `CANCELADA`; enrollment currently has no bed; bed belongs to the event's `local_id`; bed is not `bloqueada`; no other non-cancelled enrollment in the same event already references the bed; participant gender matches `quarto.genero_permitido` unless the room is `MISTO`. On success, write only `inscricoes.cama_id`. `ReleaseCamaUseCase` must clear only `inscricoes.cama_id` and keep status/pagamentos untouched.</action>
  <verify>rg -n "atribuir|DELETE /api/v1/acomodacoes/camas|transaction|cama_id" apps/api/src/modules/acomodacoes/usecases/AssignCamaUseCase.ts apps/api/src/modules/acomodacoes/usecases/ReleaseCamaUseCase.ts apps/api/src/modules/acomodacoes/routes/acomodacoes.ts</verify>
  <acceptance_criteria>
    - Assignment rejects incompatible gender with a 422-style domain error.
    - Assignment rejects occupied or blocked beds with a conflict-style error and never overwrites an existing occupant.
    - Release clears only `inscricoes.cama_id`.
    - Neither assignment nor release mutates payment history or enrollment financial status fields.
  </acceptance_criteria>
  <done>The API enforces all accommodation invariants in the backend and is safe against duplicate assignment races.</done>
</task>

<task type="auto">
  <name>Task 3: Prove map, assignment, release, and concurrency behavior end to end</name>
  <files>apps/api/src/tests/acomodacoes-operacoes.e2e.test.ts</files>
  <read_first>apps/api/src/tests/inscricoes-e2e.test.ts, apps/api/src/tests/helpers/setupTestDB.ts, apps/api/src/app.ts, apps/api/src/modules/acomodacoes/routes/acomodacoes.ts</read_first>
  <action>Create `apps/api/src/tests/acomodacoes-operacoes.e2e.test.ts`. Seed one local, two rooms with different `genero_permitido`, several beds, one event bound to that local, and male/female participants with active enrollments. Cover: map status before assignment, successful assignment, 422 on gender mismatch, conflict on occupied bed, release success, and a concurrency test that fires two assignment requests for the same bed using `Promise.allSettled` and asserts only one enrollment persists with that `cama_id` afterward.</action>
  <verify>pnpm --filter @koinonia/api exec vitest run src/tests/acomodacoes-operacoes.e2e.test.ts</verify>
  <acceptance_criteria>
    - The suite asserts all three statuses: `disponivel`, `ocupado`, and `bloqueado`.
    - The suite proves a mismatched room gender cannot be bypassed through the API.
    - The suite proves only one concurrent assignment succeeds for the same bed in the same event.
    - The test command in `<verify>` passes.
  </acceptance_criteria>
  <done>The critical backend rules of the accommodation module are regression-protected before the UI starts mutating them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @koinonia/api type-check`
- [ ] `pnpm --filter @koinonia/api exec vitest run src/tests/acomodacoes-operacoes.e2e.test.ts`
- [ ] Manual spot-check that assignment and release only touch `inscricoes.cama_id`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The backend exposes map, candidate-search, assignment, and release flows required by `ACO-04`, `ACO-05`, and `ACO-06`
- Event-scoped map operations fail fast when the event has no linked `local_id`
</success_criteria>

<output>
After completion, create `.planning/phases/03-gest-o-visual-cama-a-cama/03-02-SUMMARY.md`
</output>
