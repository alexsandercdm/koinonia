---
phase: 03-gest-o-visual-cama-a-cama
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/pages/dashboard.tsx
  - apps/web/src/pages/AcomodacoesPage.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/hooks/use-acomodacoes.ts
  - apps/web/src/components/acomodacoes/AcomodacoesFilters.tsx
  - apps/web/src/components/acomodacoes/EstruturaAcomodacaoPanel.tsx
  - apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx
  - apps/web/src/components/acomodacoes/CamaCard.tsx
autonomous: true
requirements: [ACO-01, ACO-02, ACO-03, ACO-04]
user_setup: []
must_haves:
  truths:
    - "A web app expone uma rota protegida de acomodacoes conectada ao dashboard e pronta para operacao em telas pequenas."
    - "Admin e lider conseguem cadastrar e editar local/quarto/cama pela interface sem sair do modulo."
    - "O mapa visual por evento mostra cada cama com status legivel, identificacao e ocupante quando houver."
    - "A tela funciona em modo mobile-first com filtros, cards e areas tocaveis adequadas para uso em campo."
  artifacts:
    - "apps/web/src/pages/AcomodacoesPage.tsx"
    - "apps/web/src/hooks/use-acomodacoes.ts"
    - "apps/web/src/components/acomodacoes/EstruturaAcomodacaoPanel.tsx"
    - "apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx"
    - "apps/web/src/components/acomodacoes/CamaCard.tsx"
  key_links:
    - "apps/web/src/pages/dashboard.tsx navega para `/acomodacoes`, e `apps/web/src/App.tsx` registra essa rota protegida."
    - "use-acomodacoes.ts consome os endpoints estruturais/mapa da API e invalida queries apos mutacoes cadastrais."
    - "MapaQuartosGrid renderiza o resultado de `GET /api/v1/eventos/:eventoId/mapa-acomodacao` em cards por quarto e cama."
---

<objective>
Construir a interface base do modulo de acomodacoes na web, incluindo entrada pelo dashboard, manutencao de estrutura e mapa visual somente-leitura por evento.

Purpose: disponibilizar a operacao visual da fase em uma tela unica, mobile-first, pronta para receber as acoes de atribuicao/liberacao e exportacao no proximo plan.
Output: rota `/acomodacoes`, hooks de consulta/mutacao, painel de estrutura local/quarto/cama e grade visual do mapa por quarto.
</objective>

<execution_context>
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/alexsandercdm/Projetos/koinonia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-CONTEXT.md
@.planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md
@doc/koinonia-doc-tec.md
@apps/web/src/App.tsx
@apps/web/src/pages/dashboard.tsx
@apps/web/src/components/protected-route.tsx
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/contexts/auth-context.tsx
@apps/web/package.json
@.planning/phases/03-gest-o-visual-cama-a-cama/03-01-PLAN.md

<interfaces>
From apps/web/src/components/protected-route.tsx:
- ProtectedRoute accepts `requiredRole?: string`
- `admin` bypasses narrower role checks

From apps/web/package.json:
- html2canvas and jspdf are already installed for later export work
- @tanstack/react-query v5 is available for cache/invalidation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the accommodation route, dashboard entry, and shared query client access</name>
  <files>apps/web/src/App.tsx, apps/web/src/pages/dashboard.tsx, apps/web/src/lib/api.ts, apps/web/src/hooks/use-acomodacoes.ts</files>
  <read_first>apps/web/src/App.tsx, apps/web/src/pages/dashboard.tsx, apps/web/src/components/protected-route.tsx, apps/web/src/contexts/auth-context.tsx, apps/web/src/lib/auth.ts</read_first>
  <action>Create a dedicated route for the module at `/acomodacoes` in `apps/web/src/App.tsx` and make the "Acomodacoes" dashboard CTA navigate there instead of being a dead button. Add a small fetch helper in `apps/web/src/lib/api.ts` that attaches the auth token already used by the app, then create `apps/web/src/hooks/use-acomodacoes.ts` with query/mutation hooks for `GET /api/v1/acomodacoes/locais`, `POST/PATCH` structural endpoints, and `GET /api/v1/eventos/:eventoId/mapa-acomodacao`. Configure map/list queries with `staleTime` of at least 5 minutes to respect the project's offline-grace requirement, while structural mutations must invalidate the affected local/map queries immediately.</action>
  <verify>rg -n "path=\"/acomodacoes\"|Ver Mapa|staleTime|mapa-acomodacao" apps/web/src/App.tsx apps/web/src/pages/dashboard.tsx apps/web/src/hooks/use-acomodacoes.ts apps/web/src/lib/api.ts</verify>
  <acceptance_criteria>
    - `apps/web/src/App.tsx` registers a protected `/acomodacoes` route.
    - The dashboard accommodation button navigates to `/acomodacoes`.
    - `apps/web/src/hooks/use-acomodacoes.ts` contains query hooks for structure and map endpoints.
    - Map/list queries use a non-trivial `staleTime` appropriate for unstable network conditions.
  </acceptance_criteria>
  <done>The module is reachable from the app shell and has a single place for authenticated accommodation API access.</done>
</task>

<task type="auto">
  <name>Task 2: Build the structure-management panel for local, room, and bed maintenance</name>
  <files>apps/web/src/pages/AcomodacoesPage.tsx, apps/web/src/components/acomodacoes/AcomodacoesFilters.tsx, apps/web/src/components/acomodacoes/EstruturaAcomodacaoPanel.tsx, apps/web/src/hooks/use-acomodacoes.ts</files>
  <read_first>apps/web/src/components/ui/card.tsx, apps/web/src/components/ui/input.tsx, apps/web/src/components/ui/label.tsx, packages/shared/src/index.ts, .planning/phases/03-gest-o-visual-cama-a-cama/03-01-PLAN.md</read_first>
  <action>Implement `apps/web/src/pages/AcomodacoesPage.tsx` as a mobile-first operational page with two top sections: an event filter block and a structure-management block. Create `AcomodacoesFilters` for choosing the event whose map is being viewed, and `EstruturaAcomodacaoPanel` with explicit forms/actions for local (`nome`, `endereco`, `capacidade_total`), quarto (`nome`, `genero_permitido`, `capacidade`), and cama (`identificacao`, `tipo`, `bloqueada`). Keep write actions visible only to `admin` and `lider`; for `servo`, render the structure in read-only mode. Ensure all primary buttons and tappable cards have a minimum touch target around 48px and that the mobile layout stacks vertically before any multi-column layout is used.</action>
  <verify>rg -n "capacidade_total|genero_permitido|identificacao|bloqueada|user\\?\\.role" apps/web/src/pages/AcomodacoesPage.tsx apps/web/src/components/acomodacoes/EstruturaAcomodacaoPanel.tsx apps/web/src/components/acomodacoes/AcomodacoesFilters.tsx</verify>
  <acceptance_criteria>
    - The structure panel exposes forms for local, quarto, and cama with the exact fields named in the action.
    - `servo` cannot see editable structural controls.
    - The mobile layout works as a single-column flow before expanding on wider screens.
    - Successful create/edit actions invalidate and refresh the visible structure without a full page reload.
  </acceptance_criteria>
  <done>The UI covers `ACO-01`, `ACO-02`, and `ACO-03` with practical maintenance flows aligned to role rules.</done>
</task>

<task type="auto">
  <name>Task 3: Render the read-only accommodation map as room cards with bed status</name>
  <files>apps/web/src/pages/AcomodacoesPage.tsx, apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx, apps/web/src/components/acomodacoes/CamaCard.tsx, apps/web/src/hooks/use-acomodacoes.ts</files>
  <read_first>apps/web/src/components/ui/card.tsx, packages/shared/src/index.ts, doc/koinonia-doc-tec.md, .planning/phases/03-gest-o-visual-cama-a-cama/03-RESEARCH.md</read_first>
  <action>Create `MapaQuartosGrid` to render the event-scoped map as a grid of room cards, not as a table. Each room card must show room name, allowed gender, occupancy counters, and its beds. Each `CamaCard` must show: bed identification, textual status label (`Disponivel`, `Ocupado`, `Bloqueado`), occupant name when assigned, and a high-contrast visual treatment for each status. `AcomodacoesPage.tsx` must also render explicit empty/error states when the selected event has no linked `local_id` or when the API returns no structure. Do not add assignment buttons in this plan; only pass placeholder callbacks/props that plan `03-04` can activate later.</action>
  <verify>rg -n "Disponivel|Ocupado|Bloqueado|ocupante|local_id" apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx apps/web/src/components/acomodacoes/CamaCard.tsx apps/web/src/pages/AcomodacoesPage.tsx</verify>
  <acceptance_criteria>
    - The map is rendered as room cards with nested bed cards.
    - Each bed card shows identification, status, and occupant name when present.
    - There is an explicit state for events without a linked `local_id`.
    - The page does not require manual browser refresh after structure changes or event selection changes.
  </acceptance_criteria>
  <done>The web app displays the operational map required for `ACO-04` and is ready for interactive assignment in the next wave.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @koinonia/web type-check`
- [ ] `pnpm --filter @koinonia/web build`
- [ ] Manual spot-check that `/acomodacoes` is reachable from the dashboard and renders a mobile-first map layout
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The web app supports structural CRUD flows for `ACO-01` to `ACO-03` and read-only map visualization for `ACO-04`
- The accommodation module is operable from the dashboard without dead-end UI paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-gest-o-visual-cama-a-cama/03-03-SUMMARY.md`
</output>
