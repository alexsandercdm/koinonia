---
phase: 7
plan: 07-04
title: Accent-insensitive participant search gap closure
type: execute
wave: 4
depends_on:
  - 07-03
gap_closure: true
files_modified:
  - apps/web/src/pages/ParticipantsPage.tsx
autonomous: true
requirements:
  - UI-PES-01
---

# 07-04 - Accent-insensitive participant search gap closure

<objective>
Close the Phase 7 UAT gap where searching `Joao` does not match participant names containing `João`, while preserving local cached search behavior with no per-keystroke backend refetch.
</objective>

<gap_addressed>
- UAT Test 1: Search for `Joao` or `João` should filter existing list data immediately.
</gap_addressed>

<tasks>
  <task id="07-04-T1" type="execute">
    <title>Normalize participant search text without changing query behavior</title>
    <read_first>
      - apps/web/src/pages/ParticipantsPage.tsx
      - .planning/phases/07-participantes-crud-ui/07-HUMAN-UAT.md
    </read_first>
    <action>
      In `ParticipantsPage.tsx`, add a small local `normalizeSearchText(value?: string | null)` helper that returns an empty string for missing values, applies Unicode `normalize('NFD')`, removes combining diacritics with `/[\u0300-\u036f]/g`, and lowercases the result.
      Use this helper for the search term and for every searched participant field: `nome`, `telefone`, `email`, and `padrinho_id`.
      Keep filtering inside the existing `useMemo` over cached `participantes`; do not add `search` to the query key and do not call `useParticipantes({ q: search })`.
    </action>
    <acceptance_criteria>
      - `apps/web/src/pages/ParticipantsPage.tsx` contains `normalize('NFD')`.
      - `apps/web/src/pages/ParticipantsPage.tsx` contains `/[\\u0300-\\u036f]/g`.
      - Searching logic uses `normalizeSearchText(search)` before comparing values.
      - `apps/web/src/pages/ParticipantsPage.tsx` does not contain `useParticipantes({ q: search`.
      - `pnpm --filter @koinonia/web type-check` exits 0.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Run `pnpm --filter @koinonia/web type-check`.
- Confirm `Joao` and `João` both match names such as `João`.
</verification>

<success_criteria>
- Participant search is accent-insensitive and remains local over cached data.
</success_criteria>
