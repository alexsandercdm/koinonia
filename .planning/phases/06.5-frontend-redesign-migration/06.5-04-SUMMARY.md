---
phase: 06.5-frontend-redesign-migration
plan: "04"
subsystem: ui
tags: [react, accommodation, pdf, tanstack-query, build]
requires:
  - phase: 06.5-frontend-redesign-migration
    provides: Warm shell, primitives, and migrated main pages
provides:
  - Warm accommodation page shell, tabs, filters, map states, and read-only notice
  - Warm semantic room and bed card styling
  - Final web type-check, build, and stale-token validation
  - Query data contract alignment for list responses and TanStack Query versions
affects: [accommodation-ui, frontend-redesign, frontend-verification]
tech-stack:
  added: []
  patterns:
    - Accommodation page uses shared Select, FilterTabs, and EmptyState primitives
    - List endpoints normalize array and paginated API responses through apiFetchList
key-files:
  created: []
  modified:
    - apps/web/src/pages/AcomodacoesPage.tsx
    - apps/web/src/components/acomodacoes/CamaCard.tsx
    - apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx
    - apps/web/src/lib/api.ts
    - apps/web/package.json
    - pnpm-lock.yaml
    - apps/web/src/pages/ParticipantsPage.tsx
    - apps/web/src/pages/FinanceiroPage.tsx
key-decisions:
  - "Preserved accommodation role, event, assignment, release, structure, and PDF export contracts."
  - "Pinned @tanstack/react-query to 5.99.2 to match persisted query packages and remove duplicate private QueryClient types."
  - "Added apiFetchList to support endpoints that can return either raw arrays or { data, pagination } responses."
patterns-established:
  - "Final runtime stale-token scan covers apps/web/src, Tailwind config, and global CSS."
requirements-completed: [UI-RED-01, UI-RED-02, UI-RED-03, UI-RED-04, UI-RED-05]
duration: 35min
completed: 2026-04-26
---

# Phase 6.5 Plan 04: Accommodation Migration and Final Checks Summary

**Accommodation map visuals migrated and the full web app passes type-check, build, and stale-token validation**

## Performance

- **Duration:** 35 min
- **Started:** 2026-04-26T14:20:00Z
- **Completed:** 2026-04-26T14:55:00Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Migrated `AcomodacoesPage` selector, tabs, gender filters, empty/error states, and servo read-only notice to warm-light primitives.
- Migrated accommodation room and bed map visuals to semantic success/info/warning/danger tokens.
- Preserved `canWrite`, `useMapaAcomodacao(selectedEventoId)`, `ExportMapaPdfButton`, `AssignCamaSheet`, `onAssign`, `onRelease`, `eventoId`, and `exportMapaAcomodacao`.
- Resolved final verification blockers by pinning TanStack Query versions and adding `apiFetchList` for list endpoint response normalization.
- Verified web type-check and production build pass.

## Task Commits

1. **Task 1-2: Migrate accommodation page and map components** - `52e1f76` (feat)
2. **Task 3: Final frontend non-regression fixes** - `1467ea7` (fix)

## Files Created/Modified

- `apps/web/src/pages/AcomodacoesPage.tsx` - Warm page controls and state branches.
- `apps/web/src/components/acomodacoes/CamaCard.tsx` - Semantic bed status colors.
- `apps/web/src/components/acomodacoes/MapaQuartosGrid.tsx` - Warm room cards and empty states.
- `apps/web/src/lib/api.ts` - `apiFetchList` and list response unwrapping.
- `apps/web/package.json` - TanStack React Query pinned to 5.99.2.
- `pnpm-lock.yaml` - Lockfile aligned to the installed verified dependency graph.
- `apps/web/src/pages/ParticipantsPage.tsx` - Uses `apiFetchList` for participant lists.
- `apps/web/src/pages/FinanceiroPage.tsx` - Uses `apiFetchList` for expense lists.

## Decisions Made

- Kept existing accommodation hook internals and mutation contracts intact.
- Treated the final TanStack Query type mismatch as a verification fix because the phase could not close with type-check failing.
- Kept PDF export behavior unchanged; only the visible accommodation map styling changed.

## Deviations from Plan

### Auto-fixed Issues

**1. QueryClient private type mismatch**
- **Found during:** Final frontend non-regression checks.
- **Issue:** `@tanstack/react-query` resolved to a different query-core version than persistence packages.
- **Fix:** Pinned `@tanstack/react-query` to `5.99.2` and updated lockfile.
- **Files modified:** `apps/web/package.json`, `pnpm-lock.yaml`
- **Verification:** `pnpm --filter @koinonia/web type-check` and `pnpm --filter @koinonia/web build` passed.
- **Committed in:** `1467ea7`

**2. List endpoint response shape mismatch**
- **Found during:** Final frontend non-regression checks and page data contract review.
- **Issue:** Runtime list endpoints may return arrays or `{ data, pagination }`, while pages assumed direct arrays.
- **Fix:** Added `apiFetchList` and used it in participants and finance list queries.
- **Files modified:** `apps/web/src/lib/api.ts`, `apps/web/src/pages/ParticipantsPage.tsx`, `apps/web/src/pages/FinanceiroPage.tsx`
- **Verification:** Type-check and build passed.
- **Committed in:** `1467ea7`

---

**Total deviations:** 2 auto-fixed.
**Impact on plan:** Both fixes were required to make final verification meaningful. No new dependency was added.

## Issues Encountered

None remaining after final verification fixes.

## Verification

- `pnpm --filter @koinonia/web type-check` passed.
- `pnpm --filter @koinonia/web build` passed.
- `rg "background-dark|surface-dark|surface-elevated|border-dark|border-border-dark|amber-accent|#4d0085|#1b0f23|#0f0814|#ffbf00" apps/web/src apps/web/tailwind.config.js apps/web/src/index.css` returned no matches.
- Accommodation contract greps passed for `canWrite`, `useMapaAcomodacao(selectedEventoId)`, `ExportMapaPdfButton`, `AssignCamaSheet`, `exportMapaAcomodacao`, `onAssign`, and `eventoId`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 7 can now plan and implement participant CRUD UI against the Hallowed Weight visual baseline from Phase 6.5.

---
*Phase: 06.5-frontend-redesign-migration*
*Completed: 2026-04-26*
