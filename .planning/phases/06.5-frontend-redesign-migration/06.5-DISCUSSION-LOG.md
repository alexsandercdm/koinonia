# Phase 6.5: Frontend Redesign Migration - Discussion Log

**Date:** 2026-04-26
**Mode:** `$gsd-discuss-phase` fallback/auto in Codex Execute mode

## Why This Phase Was Inserted

The user explicitly paused before Phase 7 because `doc/Koinonia-redesign` represents a significant frontend redesign, not a small adjustment. Treating it as part of Phase 7 would mix visual-system migration with participant CRUD implementation and increase regression risk.

## Gray Areas Resolved

### Redesign Scope
- **Decision:** Create a dedicated Phase 6.5 before Phase 7.
- **Rationale:** The new design changes tokens, layout, responsive behavior, primitives and page composition across the whole frontend.

### Source of Truth
- **Decision:** `doc/Koinonia-redesign` is the canonical design reference for the migration.
- **Rationale:** The Phase 5 Stitch baseline is now superseded for frontend implementation decisions.

### Migration Boundary
- **Decision:** Phase 6.5 migrates the visual system and existing pages, but does not implement new CRUD capabilities.
- **Rationale:** CRUD work remains traceable in Phases 7-10.

### Responsive Breakpoint
- **Decision:** The planner must design a new mobile breakpoint rather than relying on the existing fixed desktop sidebar.
- **Rationale:** The user described this as a "breakpoint novo" and the product must work in retreat operations on mobile.

## Assumptions Captured

- The phrase "remodelamos o design" means the current dark/violet frontend baseline should no longer be treated as the target.
- The phrase "não é ajuste" means downstream planning should avoid patch-level cosmetic changes and should plan a deliberate migration.
- The new design files are reference prototypes, not production-ready React modules to copy verbatim.

## Next Step

Run `$gsd-plan-phase 6.5` so researcher/planner can convert this context into implementation plans.
