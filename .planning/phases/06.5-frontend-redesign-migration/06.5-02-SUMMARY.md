---
phase: 06.5-frontend-redesign-migration
plan: "02"
subsystem: ui
tags: [react, routing, auth, layout, better-auth]
requires:
  - phase: 06.5-frontend-redesign-migration
    provides: Hallowed Weight tokens and shared primitives
provides:
  - Responsive warm-light authenticated shell
  - Editorial light login screen preserving Better Auth flow
  - Editorial light registration screen preserving Better Auth flow
affects: [auth, protected-routes, frontend-redesign]
tech-stack:
  added: []
  patterns:
    - Responsive app shell with desktop sidebar and mobile menu
    - Auth form styling through shared Button, Input, and FormField primitives
key-files:
  created: []
  modified:
    - apps/web/src/components/layout/AppLayout.tsx
    - apps/web/src/pages/login.tsx
    - apps/web/src/pages/register.tsx
key-decisions:
  - "Kept all existing protected route paths and AppLayout props stable."
  - "Kept Better Auth context calls untouched while replacing only presentation."
patterns-established:
  - "Authenticated shell exposes all five primary routes on mobile within one menu interaction."
  - "Auth password toggles use changing aria-label text for accessibility."
requirements-completed: [UI-RED-02, UI-RED-05]
duration: 30min
completed: 2026-04-26
---

# Phase 6.5 Plan 02: Shell and Auth Screens Summary

**Responsive warm-light application shell and Better Auth login/register screens**

## Performance

- **Duration:** 30 min
- **Started:** 2026-04-26T13:05:00Z
- **Completed:** 2026-04-26T13:35:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Migrated `AppLayout` from the fixed dark sidebar to a responsive warm shell.
- Added `Evento ativo` context affordance without introducing event CRUD or backend dependencies.
- Restyled login and registration screens around the Hallowed Weight editorial direction.
- Preserved Better Auth calls, route redirects, loading states, error states, and auth links.

## Task Commits

1. **Task 1: Convert AppLayout to responsive warm-light shell** - `e98d9d1` (feat)
2. **Task 2: Restyle login while preserving Better Auth** - `88df4d5` (feat)
3. **Task 3: Restyle register while preserving Better Auth** - `05c8612` (feat)

## Files Created/Modified

- `apps/web/src/components/layout/AppLayout.tsx` - Warm authenticated shell, desktop sidebar, mobile menu, event pill, logout.
- `apps/web/src/pages/login.tsx` - Light editorial login form using shared primitives.
- `apps/web/src/pages/register.tsx` - Matching light registration form using shared primitives.

## Decisions Made

- Used a mobile menu in the shell instead of introducing a new drawer dependency.
- Kept the event context affordance static/local for this migration, as required by the plan.
- Used existing auth context APIs unchanged: `login`, `registerUser`, loading flags, and `/dashboard` redirects.

## Deviations from Plan

None - plan executed within visual migration scope.

## Issues Encountered

- `pnpm --filter @koinonia/web type-check` still fails in `src/main.tsx` because installed TanStack Query packages resolve to both `@tanstack/query-core@5.99.0` and `@tanstack/query-core@5.99.2`. This remains outside the Wave 2 files.

## Verification

- Confirmed `AppLayout.tsx` contains `Evento ativo`, `logout`, and all five protected route paths.
- Confirmed login keeps `await login(data.email, data.password)` and `navigate('/dashboard')`.
- Confirmed register keeps `await registerUser(data.email, data.password, data.name)` and `navigate('/dashboard')`.
- Confirmed stale dark/violet token greps return no matches for Wave 2 files.
- Web type-check was attempted and failed on the existing TanStack Query version mismatch.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 06.5-03 can migrate dashboard, participants, inscrições, and financeiro pages against the new shell and primitives.

---
*Phase: 06.5-frontend-redesign-migration*
*Completed: 2026-04-26*
